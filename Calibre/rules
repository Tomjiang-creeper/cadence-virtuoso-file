RESISTANCE SHEET tndiff [6.82 0]
RESISTANCE SHEET tpdiff [7.76 0]
RESISTANCE SHEET poly [7.9 0]
RESISTANCE SHEET metal6 [0.0178 0]
RESISTANCE SHEET metal5 [0.078 0]
RESISTANCE SHEET metal4 [0.078 0]
RESISTANCE SHEET metal3 [0.078 0]
RESISTANCE SHEET metal2 [0.078 0]
RESISTANCE SHEET metal1 [0.078 0]
CAPACITANCE ORDER tndiff tpdiff gate poly metal1 metal2 metal3 metal4 metal5 metal6 
RESISTANCE CONNECTION metal1 tpdiff [0.484 0]
RESISTANCE CONNECTION metal1 tndiff [0.5324 0]
RESISTANCE CONNECTION metal1 poly [0.37752 0]
RESISTANCE CONNECTION metal2 metal1 [0.43264  0]
RESISTANCE CONNECTION metal3 metal2 [0.43264  0]
RESISTANCE CONNECTION metal4 metal3 [0.43264  0]
RESISTANCE CONNECTION metal5 metal4 [0.43264  0]
RESISTANCE CONNECTION metal6 metal5 [0.329184  0]
//
// Generated on Tue Jun  8 03:05:38 2004 by xCalibrate v9.1_1.1 Tue Jan 8 09:53:43 PST 2002
//
// Comments: 
//
// Technology name: t018s6mm_rf
// Precision: 1000
// DBU Size: 1e-09
// DB Magnification: 1
// Capacitance Units: ff
// Background Dielectric: 1
// Stacking Mode: Unconditional
//
// Following line(s) should be in your rulesfile
// UNIT CAPACITANCE ff
// Above line(s) should be in your rulesfile
//
// 
// Profile = field
// Layers =  (field_base field_base_diel poly poly_diel poly_diel2 metal1 metal1_diel metal1_diel2 metal2 metal2_diel metal2_diel2 metal3 metal3_diel metal3_diel2 metal4 metal4_diel metal4_diel2 metal5 metal5_diel metal5_diel2 metal6 metal6_diel metal6_diel2 metal6_diel3)
// 
//     Z-COORD        NAME TYPE THICKNESS   DIEL   WIDTH   SPACE      WIDTH   SPACE    BIAS 
//                                                 ACTUAL  ACTUAL     DRAWN   DRAWN          
//  --- 11.4000 ------------------------------------------------------------------------------
//  |         metal6_diel3   D    0.6000   7.9000      
//  --- 10.8000 ------------------------------------------------------------------------------
//  |         metal6_diel2   D    0.1500   4.2000      
//  --- 10.6500 ------------------------------------------------------------------------------
//  |          metal6_diel   D    0.1600   4.2000      
//  --- 10.4900 ------------------------------------------------------------------------------
//  |               metal6   C    2.3400           1.5000  1.5000 
//  --- 8.1500  ------------------------------------------------------------------------------
//  |         metal5_diel2   D    0.3500   4.2000      
//  --- 7.8000  ------------------------------------------------------------------------------
//  |          metal5_diel   D    0.6500   3.7000      
//  --- 7.1500  ------------------------------------------------------------------------------
//  |               metal5   C    0.5300           0.2800  0.2800 
//  --- 6.6200  ------------------------------------------------------------------------------
//  |         metal4_diel2   D    0.2000   4.2000      
//  --- 6.4200  ------------------------------------------------------------------------------
//  |          metal4_diel   D    0.6500   3.7000      
//  --- 5.7700  ------------------------------------------------------------------------------
//  |               metal4   C    0.5300           0.2800  0.2800 
//  --- 5.2400  ------------------------------------------------------------------------------
//  |         metal3_diel2   D    0.2000   4.2000      
//  --- 5.0400  ------------------------------------------------------------------------------
//  |          metal3_diel   D    0.6500   3.7000      
//  --- 4.3900  ------------------------------------------------------------------------------
//  |               metal3   C    0.5300           0.2800  0.2800 
//  --- 3.8600  ------------------------------------------------------------------------------
//  |         metal2_diel2   D    0.2000   4.2000      
//  --- 3.6600  ------------------------------------------------------------------------------
//  |          metal2_diel   D    0.6500   3.7000      
//  --- 3.0100  ------------------------------------------------------------------------------
//  |               metal2   C    0.5300           0.2800  0.2800 
//  --- 2.4800  ------------------------------------------------------------------------------
//  |         metal1_diel2   D    0.2000   4.2000      
//  --- 2.2800  ------------------------------------------------------------------------------
//  |          metal1_diel   D    0.6500   3.7000      
//  --- 1.6300  ------------------------------------------------------------------------------
//  |               metal1   C    0.5300           0.2300  0.2300 
//  --- 1.1000  ------------------------------------------------------------------------------
//  |           poly_diel2   D    0.5000   4.0000      
//  --- 0.6000  ------------------------------------------------------------------------------
//  |            poly_diel   D    0.0000   4.0000      
//  --- 0.6000  ------------------------------------------------------------------------------
//  |                 poly   C    0.2000           0.1600  0.2700     0.1800  0.2500   -0.0200
//  --- 0.4000  ------------------------------------------------------------------------------
//  |      field_base_diel   D    0.4000   3.9000      
//  --- 0.0000  ------------------------------------------------------------------------------
//  |           field_base   B    1.0000
//  --- -1.0000 ------------------------------------------------------------------------------
// 
// Profile = active
// Layers =  (active_base active_base_diel metal1 metal1_diel metal1_diel2 metal2 metal2_diel metal2_diel2 metal3 metal3_diel metal3_diel2 metal4 metal4_diel metal4_diel2 metal5 metal5_diel metal5_diel2 metal6 metal6_diel metal6_diel2 metal6_diel3)
// 
//     Z-COORD        NAME TYPE THICKNESS   DIEL   WIDTH   SPACE  
//                                                                 
//  --- 11.0000 ---------------------------------------------------
//  |         metal6_diel3   D    0.6000   7.9000      
//  --- 10.4000 ---------------------------------------------------
//  |         metal6_diel2   D    0.1500   4.2000      
//  --- 10.2500 ---------------------------------------------------
//  |          metal6_diel   D    0.1600   4.2000      
//  --- 10.0900 ---------------------------------------------------
//  |               metal6   C    2.3400           1.5000  1.5000 
//  --- 7.7500  ---------------------------------------------------
//  |         metal5_diel2   D    0.3500   4.2000      
//  --- 7.4000  ---------------------------------------------------
//  |          metal5_diel   D    0.6500   3.7000      
//  --- 6.7500  ---------------------------------------------------
//  |               metal5   C    0.5300           0.2800  0.2800 
//  --- 6.2200  ---------------------------------------------------
//  |         metal4_diel2   D    0.2000   4.2000      
//  --- 6.0200  ---------------------------------------------------
//  |          metal4_diel   D    0.6500   3.7000      
//  --- 5.3700  ---------------------------------------------------
//  |               metal4   C    0.5300           0.2800  0.2800 
//  --- 4.8400  ---------------------------------------------------
//  |         metal3_diel2   D    0.2000   4.2000      
//  --- 4.6400  ---------------------------------------------------
//  |          metal3_diel   D    0.6500   3.7000      
//  --- 3.9900  ---------------------------------------------------
//  |               metal3   C    0.5300           0.2800  0.2800 
//  --- 3.4600  ---------------------------------------------------
//  |         metal2_diel2   D    0.2000   4.2000      
//  --- 3.2600  ---------------------------------------------------
//  |          metal2_diel   D    0.6500   3.7000      
//  --- 2.6100  ---------------------------------------------------
//  |               metal2   C    0.5300           0.2800  0.2800 
//  --- 2.0800  ---------------------------------------------------
//  |         metal1_diel2   D    0.2000   4.2000      
//  --- 1.8800  ---------------------------------------------------
//  |          metal1_diel   D    0.6500   3.7000      
//  --- 1.2300  ---------------------------------------------------
//  |               metal1   C    0.5300           0.2300  0.2300 
//  --- 0.7000  ---------------------------------------------------
//  |     active_base_diel   D    0.7000   4.0000      
//  --- 0.0000  ---------------------------------------------------
//  |          active_base   B    1.0000
//  --- -1.0000 ---------------------------------------------------
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,poly
//
CAPACITANCE INTRINSIC PLATE poly MASK
[
    PROPERTY C
    C = 0.0863267 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal1
//
CAPACITANCE INTRINSIC PLATE metal1 MASK
[
    PROPERTY C
    C = 0.031899 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal2
//
CAPACITANCE INTRINSIC PLATE metal2 MASK
[
    PROPERTY C
    C = 0.0137477 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal3
//
CAPACITANCE INTRINSIC PLATE metal3 MASK
[
    PROPERTY C
    C = 0.00876268 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal4
//
CAPACITANCE INTRINSIC PLATE metal4 MASK
[
    PROPERTY C
    C = 0.00643105 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal5
//
CAPACITANCE INTRINSIC PLATE metal5 MASK
[
    PROPERTY C
    C = 0.00507972 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal6
//
CAPACITANCE INTRINSIC PLATE metal6 MASK
[
    PROPERTY C
    C = 0.00412776 * area()
]
//
//  model: 34  target: intrinsic  profile: field  layers: field_base,poly
//
CAPACITANCE INTRINSIC FRINGE poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0412511 * (1 - exp(-1.34417 * (distance() - -0.02 + 0.0943919)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0435317
    }
]
//
//  model: 34  target: nearbody  profile: field  layers: field_base,poly
//
CAPACITANCE NEARBODY poly WITH SHIELD poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.47118 - 1.52371 * (distance() - -0.02)) + 0.0100325 / pow(distance() - -0.02 , 1.08481)) * 1.05829 * pow((width1() + width2()) / 2 + -0.02 , 0.163408)
]
//
//  model: 34  target: intrinsic  profile: field  layers: field_base,metal1
//
CAPACITANCE INTRINSIC FRINGE metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0369188 * (1 - exp(-0.597508 * (distance() + 0.199926)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0401092
    }
]
//
//  model: 34  target: nearbody  profile: field  layers: field_base,metal1
//
CAPACITANCE NEARBODY metal1 WITH SHIELD metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.78114 - 0.835412 * (distance())) + 0.0185203 / pow(distance() , 0.970137)) * 1.343 * pow((width1() + width2()) / 2 , 0.109054)
]
//
//  model: 34  target: intrinsic  profile: field  layers: field_base,metal2
//
CAPACITANCE INTRINSIC FRINGE metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0281418 * (1 - exp(-0.360124 * (distance() + 0.503243)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0321452
    }
]
//
//  model: 34  target: nearbody  profile: field  layers: field_base,metal2
//
CAPACITANCE NEARBODY metal2 WITH SHIELD metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.5475 - 0.445798 * (distance())) + 0.0170042 / pow(distance() , 0.862093)) * 1.9478 * pow((width1() + width2()) / 2 , 0.134699)
]
//
//  model: 34  target: intrinsic  profile: field  layers: field_base,metal3
//
CAPACITANCE INTRINSIC FRINGE metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0225181 * (1 - exp(-0.316326 * (distance() + 0.728714)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0284276
    }
]
//
//  model: 34  target: nearbody  profile: field  layers: field_base,metal3
//
CAPACITANCE NEARBODY metal3 WITH SHIELD metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.66941 - 0.276987 * (distance())) + 0.0206025 / pow(distance() , 0.821823)) * 1.78239 * pow((width1() + width2()) / 2 , 0.144691)
]
//
//  model: 34  target: intrinsic  profile: field  layers: field_base,metal4
//
CAPACITANCE INTRINSIC FRINGE metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0182412 * (1 - exp(-0.326855 * (distance() + 0.841968)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0259925
    }
]
//
//  model: 34  target: nearbody  profile: field  layers: field_base,metal4
//
CAPACITANCE NEARBODY metal4 WITH SHIELD metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.704 - 0.188358 * (distance())) + 0.0219844 / pow(distance() , 0.807488)) * 1.73251 * pow((width1() + width2()) / 2 , 0.149355)
]
//
//  model: 34  target: intrinsic  profile: field  layers: field_base,metal5
//
CAPACITANCE INTRINSIC FRINGE metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0151109 * (1 - exp(-0.354137 * (distance() + 0.903379)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0241517
    }
]
//
//  model: 34  target: nearbody  profile: field  layers: field_base,metal5
//
CAPACITANCE NEARBODY metal5 WITH SHIELD metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.65431 - 0.138634 * (distance())) + 0.0215108 / pow(distance() , 0.811731)) * 1.76025 * pow((width1() + width2()) / 2 , 0.153195)
]
//
//  model: 34  target: intrinsic  profile: field  layers: field_base,metal6
//
CAPACITANCE INTRINSIC FRINGE metal6 MASK
[
    PROPERTY C
    max_caldistance = 15 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0382469 * (1 - exp(-0.0525467 * (distance() + 1.05983)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0286051
    }
]
//
//  model: 34  target: nearbody  profile: field  layers: field_base,metal6
//
CAPACITANCE NEARBODY metal6 WITH SHIELD metal6 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 15 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.70795 - 0.239515 * (distance())) + 0.0242853 / pow(distance() , 55.0592)) * 1.9193 * pow((width1() + width2()) / 2 , 0.0130894)
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE CROSSOVER PLATE poly metal1 MASK
[
    PROPERTY C
    C = 0.070832 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE CROSSOVER PLATE poly metal2 MASK
[
    PROPERTY C
    C = 0.0180159 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE CROSSOVER PLATE poly metal3 MASK
[
    PROPERTY C
    C = 0.0103222 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE CROSSOVER PLATE poly metal4 MASK
[
    PROPERTY C
    C = 0.00723347 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE CROSSOVER PLATE poly metal5 MASK
[
    PROPERTY C
    C = 0.00556809 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE CROSSOVER PLATE poly metal6 MASK
[
    PROPERTY C
    C = 0.00444472 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE CROSSOVER PLATE metal1 metal2 MASK
[
    PROPERTY C
    C = 0.039657 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE CROSSOVER PLATE metal1 metal3 MASK
[
    PROPERTY C
    C = 0.0150164 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE CROSSOVER PLATE metal1 metal4 MASK
[
    PROPERTY C
    C = 0.00926255 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE CROSSOVER PLATE metal1 metal5 MASK
[
    PROPERTY C
    C = 0.00669726 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE CROSSOVER PLATE metal1 metal6 MASK
[
    PROPERTY C
    C = 0.00513598 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE CROSSOVER PLATE metal2 metal3 MASK
[
    PROPERTY C
    C = 0.0396571 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE CROSSOVER PLATE metal2 metal4 MASK
[
    PROPERTY C
    C = 0.0150164 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE CROSSOVER PLATE metal2 metal5 MASK
[
    PROPERTY C
    C = 0.00926255 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE CROSSOVER PLATE metal2 metal6 MASK
[
    PROPERTY C
    C = 0.0065212 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE CROSSOVER PLATE metal3 metal4 MASK
[
    PROPERTY C
    C = 0.039657 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE CROSSOVER PLATE metal3 metal5 MASK
[
    PROPERTY C
    C = 0.0150164 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE CROSSOVER PLATE metal3 metal6 MASK
[
    PROPERTY C
    C = 0.00892915 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE CROSSOVER PLATE metal4 metal5 MASK
[
    PROPERTY C
    C = 0.039657 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE CROSSOVER PLATE metal4 metal6 MASK
[
    PROPERTY C
    C = 0.014159 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE CROSSOVER PLATE metal5 metal6 MASK
[
    PROPERTY C
    C = 0.0341888 * area()
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE NEARBODY poly INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.58089 - 2.93082 * (distance() - -0.02)) + 0.00114313 / pow(distance() - -0.02 , 1.8332)) * 1.01908 * pow((width1() + width2()) / 2 + -0.02 , 0.0687971)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE CROSSOVER FRINGE poly metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0215127 * (1 - exp(-2.36433 * (distance() - -0.02 + -0.0317567)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0223861
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00289069 * (distance() - -0.02) + 0.290157) * (0.152182 - exp(-(-0.597344 * (distance() - -0.02) + 0.355652) * (enclose() - -0.02 + 3.19891 * (distance() - -0.02) + 2.91309) - (0.00669819 * (distance() - -0.02) + 0.199517) * (enclose() - -0.02 + 2.65421 * (distance() - -0.02) + 1.72817) * (enclose() - -0.02 + 2.65421 * (distance() - -0.02) + 1.72817)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0215127 * (1 - exp(-2.36433 * (distance() - -0.02 + -0.0317567)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.137852 * (0.152182 - exp(2.63107 * (enclose() - -0.02 + 18.9076) - 0.233008 * (enclose() - -0.02 + 14.9992) * (enclose() - -0.02 + 14.9992)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0252684 * (1 - exp(-2.39228 * (distance() - -0.02 + -0.0301879)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0262623
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE NEARBODY poly INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.04886 - 1.72513 * (distance() - -0.02)) + 0.00349726 / pow(distance() - -0.02 , 1.47702)) * 1.12632 * pow((width1() + width2()) / 2 + -0.02 , 0.133687)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE CROSSOVER FRINGE poly metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00840406 * (1 - exp(-1.43588 * (distance() - -0.02 + 0.0497259)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00897179
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0010772 * (distance() - -0.02) + 0.0395648) * (0.47186 - exp(-(0.262914 * (distance() - -0.02) + 0.299283) * (enclose() - -0.02 + -0.423869 * (distance() - -0.02) + 1.78982) - (7.74525e-06 * (distance() - -0.02) + 6.32675e-05) * (enclose() - -0.02 + 22.1882 * (distance() - -0.02) + 24.7231) * (enclose() - -0.02 + 22.1882 * (distance() - -0.02) + 24.7231)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00840406 * (1 - exp(-1.43588 * (distance() - -0.02 + 0.0497259)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0170894 * (0.47186 - exp(-1.61385 * (enclose() - -0.02 + -0.329525) - 0.000101994 * (enclose() - -0.02 + 135.664) * (enclose() - -0.02 + 135.664)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0339884 * (1 - exp(-1.52183 * (distance() - -0.02 + 0.0480879)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0360499
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE NEARBODY poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.16623 - 1.50188 * (distance() - -0.02)) + 0.00600816 / pow(distance() - -0.02 , 1.32171)) * 1.02773 * pow((width1() + width2()) / 2 + -0.02 , 0.152587)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE CROSSOVER FRINGE poly metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0052819 * (1 - exp(-1.31299 * (distance() - -0.02 + 0.0803239)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00571241
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00076948 * (distance() - -0.02) + 0.0204163) * (0.613676 - exp(-(0.0975662 * (distance() - -0.02) + 0.171791) * (enclose() - -0.02 + 0.449706 * (distance() - -0.02) + 2.12662) - (0.00018337 * (distance() - -0.02) + -0.0113586) * (enclose() - -0.02 + -2.51307 * (distance() - -0.02) + -0.92197) * (enclose() - -0.02 + -2.51307 * (distance() - -0.02) + -0.92197)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0052819 * (1 - exp(-1.31299 * (distance() - -0.02 + 0.0803239)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00828445 * (0.613676 - exp(-0.659622 * (enclose() - -0.02 + 4.37515) - -0.0104417 * (enclose() - -0.02 + -13.4873) * (enclose() - -0.02 + -13.4873)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0365069 * (1 - exp(-1.39832 * (distance() - -0.02 + 0.0759251)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0391709
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE NEARBODY poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.18275 - 1.46499 * (distance() - -0.02)) + 0.00798806 / pow(distance() - -0.02 , 1.23169)) * 0.946094 * pow((width1() + width2()) / 2 + -0.02 , 0.155134)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE CROSSOVER FRINGE poly metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00385473 * (1 - exp(-1.27598 * (distance() - -0.02 + 0.0917757)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00420398
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00036432 * (distance() - -0.02) + 0.0174208) * (0.467644 - exp(-(-0.0872244 * (distance() - -0.02) + 0.0949489) * (enclose() - -0.02 + 4.31278 * (distance() - -0.02) + 5.91179) - (2.66598e-05 * (distance() - -0.02) + 0.0230818) * (enclose() - -0.02 + 3.9263 * (distance() - -0.02) + 1.68325) * (enclose() - -0.02 + 3.9263 * (distance() - -0.02) + 1.68325)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00385473 * (1 - exp(-1.27598 * (distance() - -0.02 + 0.0917757)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0077996 * (0.467644 - exp(0.341173 * (enclose() - -0.02 + 27.4757) - 0.0232151 * (enclose() - -0.02 + 21.3148) * (enclose() - -0.02 + 21.3148)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0377007 * (1 - exp(-1.36074 * (distance() - -0.02 + 0.0859892)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0407648
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE NEARBODY poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.31635 - 1.45965 * (distance() - -0.02)) + 0.00829272 / pow(distance() - -0.02 , 1.1872)) * 1.0194 * pow((width1() + width2()) / 2 + -0.02 , 0.15973)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE CROSSOVER FRINGE poly metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00303438 * (1 - exp(-1.27483 * (distance() - -0.02 + 0.0914924)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00333186
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00064717 * (distance() - -0.02) + 0.0131665) * (0.648655 - exp(-(0.023101 * (distance() - -0.02) + 0.168777) * (enclose() - -0.02 + 0.281266 * (distance() - -0.02) + 2.08051) - (0.00286331 * (distance() - -0.02) + -0.0281347) * (enclose() - -0.02 + -0.973857 * (distance() - -0.02) + 0.774236) * (enclose() - -0.02 + -0.973857 * (distance() - -0.02) + 0.774236)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00303438 * (1 - exp(-1.27483 * (distance() - -0.02 + 0.0914924)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00496532 * (0.648655 - exp(-0.284282 * (enclose() - -0.02 + 3.48684) - -0.0138181 * (enclose() - -0.02 + -4.09505) * (enclose() - -0.02 + -4.09505)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0383838 * (1 - exp(-1.35742 * (distance() - -0.02 + 0.0864101)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.041764
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE NEARBODY poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.38206 - 1.46907 * (distance() - -0.02)) + 0.00856439 / pow(distance() - -0.02 , 1.1587)) * 1.0515 * pow((width1() + width2()) / 2 + -0.02 , 0.159429)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE CROSSOVER FRINGE poly metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00246317 * (1 - exp(-1.25849 * (distance() - -0.02 + 0.0971925)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00271307
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00050499 * (distance() - -0.02) + 0.0101777) * (0.669784 - exp(-(0.0315978 * (distance() - -0.02) + 0.12477) * (enclose() - -0.02 + 0.0149012 * (distance() - -0.02) + 2.5128) - (0.00376558 * (distance() - -0.02) + -0.0179714) * (enclose() - -0.02 + -0.72167 * (distance() - -0.02) + 0.794597) * (enclose() - -0.02 + -0.72167 * (distance() - -0.02) + 0.794597)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00246317 * (1 - exp(-1.25849 * (distance() - -0.02 + 0.0971925)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00382637 * (0.669784 - exp(-0.282759 * (enclose() - -0.02 + 2.58731) - 0.0008565 * (enclose() - -0.02 + -2.81375) * (enclose() - -0.02 + -2.81375)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0389075 * (1 - exp(-1.34097 * (distance() - -0.02 + 0.0916382)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0424559
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.14577 - 1.47152 * (distance())) + 0.00321417 / pow(distance() , 1.52857)) * 1.89008 * pow((width1() + width2()) / 2 , 0.0610007)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE CROSSOVER FRINGE metal1 metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0257318 * (1 - exp(-0.969945 * (distance() + 0.0146975)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0263769
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00104861 * (distance()) + 0.517384) * (0.0966853 - exp(-(-0.0173302 * (distance()) + 0.0063937) * (enclose() + 17.2129 * (distance()) + 43.3385) - (0.0107658 * (distance()) + 0.0223313) * (enclose() + 0.816469 * (distance()) + 9.17504) * (enclose() + 0.816469 * (distance()) + 9.17504)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0257318 * (1 - exp(-0.969945 * (distance() + 0.0146975)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.25607 * (0.0966853 - exp(0.0802573 * (enclose() + 129.403) - 0.0761603 * (enclose() + 13.2574) * (enclose() + 13.2574)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0218354 * (1 - exp(-0.941997 * (distance() + 0.0194852)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0223951
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.13728 - 1.06012 * (distance())) + 0.00759162 / pow(distance() , 1.30107)) * 1.44594 * pow((width1() + width2()) / 2 , 0.0861446)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE CROSSOVER FRINGE metal1 metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0145382 * (1 - exp(-0.648809 * (distance() + 0.107656)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0148719
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00016161 * (distance()) + 0.0352069) * (0.8232 - exp(-(0.325151 * (distance()) + 0.235956) * (enclose() + 0.22082 * (distance()) + 0.696673) - (-0.00947369 * (distance()) + -0.0343877) * (enclose() + 0.837051 * (distance()) + 1.48542) * (enclose() + 0.837051 * (distance()) + 1.48542)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0145382 * (1 - exp(-0.648809 * (distance() + 0.107656)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0171994 * (0.8232 - exp(-1.86171 * (enclose() + 1.80077) - -0.0817561 * (enclose() + 5.67067) * (enclose() + 5.67067)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0276793 * (1 - exp(-0.68681 * (distance() + 0.0936463)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0284119
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.30989 - 0.93516 * (distance())) + 0.0102972 / pow(distance() , 1.18183)) * 1.43154 * pow((width1() + width2()) / 2 , 0.0958292)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE CROSSOVER FRINGE metal1 metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0102304 * (1 - exp(-0.578141 * (distance() + 0.157658)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0106457
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00037883 * (distance()) + 0.0252358) * (0.880972 - exp(-(0.201741 * (distance()) + 0.183553) * (enclose() + 0.333847 * (distance()) + 0.755597) - (-0.00353215 * (distance()) + -0.0226295) * (enclose() + 1.1159 * (distance()) + 2.02538) * (enclose() + 1.1159 * (distance()) + 2.02538)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0102304 * (1 - exp(-0.578141 * (distance() + 0.157658)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0116708 * (0.880972 - exp(-1.19226 * (enclose() + 2.42483) - -0.0402902 * (enclose() + 7.60488) * (enclose() + 7.60488)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0302173 * (1 - exp(-0.62345 * (distance() + 0.134087)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0315555
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.47833 - 0.88186 * (distance())) + 0.0117331 / pow(distance() , 1.1148)) * 1.48435 * pow((width1() + width2()) / 2 , 0.101283)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE CROSSOVER FRINGE metal1 metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0078677 * (1 - exp(-0.557903 * (distance() + 0.184128)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00833199
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00071443 * (distance()) + 0.0223345) * (0.89548 - exp(-(0.129186 * (distance()) + 0.150351) * (enclose() + 0.341818 * (distance()) + 0.94088) - (-0.00156558 * (distance()) + -0.0167732) * (enclose() + 1.16042 * (distance()) + 2.38624) * (enclose() + 1.16042 * (distance()) + 2.38624)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0078677 * (1 - exp(-0.557903 * (distance() + 0.184128)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00938118 * (0.89548 - exp(-0.796281 * (enclose() + 2.64997) - -0.0246011 * (enclose() + 8.18834) * (enclose() + 8.18834)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0315356 * (1 - exp(-0.604637 * (distance() + 0.155049)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0334757
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.45094 - 0.853753 * (distance())) + 0.0146471 / pow(distance() , 1.07566)) * 1.31123 * pow((width1() + width2()) / 2 , 0.102788)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE CROSSOVER FRINGE metal1 metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00628045 * (1 - exp(-0.550908 * (distance() + 0.199824)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00674835
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00076974 * (distance()) + 0.0183156) * (0.976408 - exp(-(0.0819505 * (distance()) + 0.134541) * (enclose() + 0.4907 * (distance()) + 1.43494) - (-0.000741 * (distance()) + -0.00784846) * (enclose() + 1.39711 * (distance()) + 5.36877) * (enclose() + 1.39711 * (distance()) + 5.36877)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00628045 * (1 - exp(-0.550908 * (distance() + 0.199824)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00723345 * (0.976408 - exp(-0.544293 * (enclose() + 3.88844) - -0.0115535 * (enclose() + 12.3543) * (enclose() + 12.3543)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0324469 * (1 - exp(-0.597822 * (distance() + 0.167727)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0349003
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.93611 - 1.0879 * (distance())) + 0.00869762 / pow(distance() , 1.35419)) * 1.17797 * pow((width1() + width2()) / 2 , 0.0795119)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal2 metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0309854 * (1 - exp(-0.761032 * (distance() + 0.0565078)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0319635
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0213781 * (distance()) + 1.08431) * (0.049494 - exp(-(-0.190229 * (distance()) + 0.404541) * (enclose() + 1.41694 * (distance()) + 6.68007) - (0.0200406 * (distance()) + 0.00196033) * (enclose() + 0.511414 * (distance()) + 7.89501) * (enclose() + 0.511414 * (distance()) + 7.89501)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0309854 * (1 - exp(-0.761032 * (distance() + 0.0565078)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.5956 * (0.049494 - exp(0.546604 * (enclose() + 13.7648) - 0.102163 * (enclose() + 10.4521) * (enclose() + 10.4521)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE INTRINSIC FRINGE metal2 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0128671 * (1 - exp(-0.692152 * (distance() + 0.0696691)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0132232
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.09308 - 0.746764 * (distance())) + 0.013233 / pow(distance() , 1.19624)) * 1.14764 * pow((width1() + width2()) / 2 , 0.107205)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal2 metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.019625 * (1 - exp(-0.437976 * (distance() + 0.232517)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0197292
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00174687 * (distance()) + 0.0370865) * (0.802138 - exp(-(0.292087 * (distance()) + 0.262515) * (enclose() + 0.260494 * (distance()) + 1.13078) - (-0.00994109 * (distance()) + -0.015613) * (enclose() + 0.747553 * (distance()) + 3.55286) * (enclose() + 0.747553 * (distance()) + 3.55286)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.019625 * (1 - exp(-0.437976 * (distance() + 0.232517)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0229104 * (0.802138 - exp(-1.72295 * (enclose() + 2.43325) - -0.0653185 * (enclose() + 7.29063) * (enclose() + 7.29063)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE INTRINSIC FRINGE metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0182451 * (1 - exp(-0.434663 * (distance() + 0.229572)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0182975
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.35624 - 0.627597 * (distance())) + 0.0153747 / pow(distance() , 1.09183)) * 1.25546 * pow((width1() + width2()) / 2 , 0.118183)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal2 metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0146129 * (1 - exp(-0.361385 * (distance() + 0.345691)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0147318
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0001003 * (distance()) + 0.0347541) * (0.676671 - exp(-(0.0593957 * (distance()) + 0.190124) * (enclose() + -0.0489327 * (distance()) + 1.95852) - (0.00798872 * (distance()) + -0.0196241) * (enclose() + 0.0691267 * (distance()) + 1.98777) * (enclose() + 0.0691267 * (distance()) + 1.98777)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0146129 * (1 - exp(-0.361385 * (distance() + 0.345691)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0176278 * (0.676671 - exp(-0.487102 * (enclose() + 1.71386) - 0.0203195 * (enclose() + 2.3334) * (enclose() + 2.3334)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE INTRINSIC FRINGE metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0210179 * (1 - exp(-0.366812 * (distance() + 0.333509)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.021213
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.66377 - 0.565448 * (distance())) + 0.0154045 / pow(distance() , 1.02413)) * 1.46819 * pow((width1() + width2()) / 2 , 0.124248)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal2 metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0112494 * (1 - exp(-0.341404 * (distance() + 0.408671)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0116687
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00060851 * (distance()) + 0.0283024) * (0.938068 - exp(-(0.0919391 * (distance()) + 0.128095) * (enclose() + 0.494591 * (distance()) + 0.967393) - (-0.00092717 * (distance()) + -0.0112135) * (enclose() + 1.36073 * (distance()) + 3.21987) * (enclose() + 1.36073 * (distance()) + 3.21987)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0112494 * (1 - exp(-0.341404 * (distance() + 0.408671)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0126299 * (0.938068 - exp(-0.58779 * (enclose() + 3.44035) - -0.0158493 * (enclose() + 10.0235) * (enclose() + 10.0235)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE INTRINSIC FRINGE metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0224875 * (1 - exp(-0.349453 * (distance() + 0.389105)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0233562
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.04242 - 0.972748 * (distance())) + 0.012687 / pow(distance() , 1.22661)) * 1.09148 * pow((width1() + width2()) / 2 , 0.0910613)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0332621 * (1 - exp(-0.705628 * (distance() + 0.0886863)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0349265
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0380738 * (distance()) + 1.12744) * (0.0476352 - exp(-(-0.13317 * (distance()) + 0.247678) * (enclose() + 2.01954 * (distance()) + 8.24614) - (0.0139098 * (distance()) + 0.0109627) * (enclose() + 0.641145 * (distance()) + 8.66018) * (enclose() + 0.641145 * (distance()) + 8.66018)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0332621 * (1 - exp(-0.705628 * (distance() + 0.0886863)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.658905 * (0.0476352 - exp(0.418172 * (enclose() + 18.3438) - 0.0805117 * (enclose() + 11.8659) * (enclose() + 11.8659)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE INTRINSIC FRINGE metal3 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.00913654 * (1 - exp(-0.632198 * (distance() + 0.110855)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.00956161
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.50156 - 0.639325 * (distance())) + 0.0130763 / pow(distance() , 1.09654)) * 1.4598 * pow((width1() + width2()) / 2 , 0.117031)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0220995 * (1 - exp(-0.382559 * (distance() + 0.317143)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0224883
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00226099 * (distance()) + 0.0436671) * (0.673019 - exp(-(0.134457 * (distance()) + 0.180736) * (enclose() + 0.0187914 * (distance()) + 1.5218) - (-1.01464e-05 * (distance()) + -7.69659e-07) * (enclose() + -24.4465 * (distance()) + 29.2333) * (enclose() + -24.4465 * (distance()) + 29.2333)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0220995 * (1 - exp(-0.382559 * (distance() + 0.317143)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.027486 * (0.673019 - exp(-0.853021 * (enclose() + 1.61576) - -5.15017e-05 * (enclose() + -92.9992) * (enclose() + -92.9992)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE INTRINSIC FRINGE metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0136104 * (1 - exp(-0.372951 * (distance() + 0.325919)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0137883
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.56488 - 0.510873 * (distance())) + 0.0182839 / pow(distance() , 1.01001)) * 1.28641 * pow((width1() + width2()) / 2 , 0.128825)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0163994 * (1 - exp(-0.308772 * (distance() + 0.47995)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0169424
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00097592 * (distance()) + 0.028689) * (0.87958 - exp(-(0.142265 * (distance()) + 0.157244) * (enclose() + 0.447487 * (distance()) + 0.723659) - (-0.00218016 * (distance()) + -0.0150786) * (enclose() + 1.31853 * (distance()) + 2.07968) * (enclose() + 1.31853 * (distance()) + 2.07968)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0163994 * (1 - exp(-0.308772 * (distance() + 0.47995)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0167843 * (0.87958 - exp(-0.868569 * (enclose() + 2.96109) - -0.0259794 * (enclose() + 8.67233) * (enclose() + 8.67233)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE INTRINSIC FRINGE metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0161667 * (1 - exp(-0.308482 * (distance() + 0.474615)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0166866
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.32619 - 0.92771 * (distance())) + 0.0128436 / pow(distance() , 1.15456)) * 1.27649 * pow((width1() + width2()) / 2 , 0.0972917)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0344711 * (1 - exp(-0.689687 * (distance() + 0.104706)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0367665
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0128198 * (distance()) + 0.303588) * (0.175418 - exp(-(-0.107856 * (distance()) + 0.199571) * (enclose() + 2.12128 * (distance()) + 4.46183) - (0.0167011 * (distance()) + 0.0230708) * (enclose() + 0.598271 * (distance()) + 5.38414) * (enclose() + 0.598271 * (distance()) + 5.38414)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0344711 * (1 - exp(-0.689687 * (distance() + 0.104706)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.183843 * (0.175418 - exp(0.339709 * (enclose() + 15.0682) - 0.106576 * (enclose() + 8.3755) * (enclose() + 8.3755)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE INTRINSIC FRINGE metal4 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.00706672 * (1 - exp(-0.615619 * (distance() + 0.131922)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.00752228
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.64732 - 0.573067 * (distance())) + 0.0157067 / pow(distance() , 1.02308)) * 1.4421 * pow((width1() + width2()) / 2 , 0.124015)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0227679 * (1 - exp(-0.355106 * (distance() + 0.383894)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0237087
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00308043 * (distance()) + 0.0349333) * (0.772988 - exp(-(0.225188 * (distance()) + 0.210077) * (enclose() + 0.393323 * (distance()) + 1.10015) - (-0.00571376 * (distance()) + -0.00550384) * (enclose() + 1.10133 * (distance()) + 4.19574) * (enclose() + 1.10133 * (distance()) + 4.19574)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0227679 * (1 - exp(-0.355106 * (distance() + 0.383894)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0251677 * (0.772988 - exp(-1.33602 * (enclose() + 3.06677) - -0.0340726 * (enclose() + 9.70239) * (enclose() + 9.70239)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE INTRINSIC FRINGE metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0110213 * (1 - exp(-0.346109 * (distance() + 0.396824)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0114455
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.40691 - 0.855413 * (distance())) + 0.0149415 / pow(distance() , 1.0934)) * 1.25884 * pow((width1() + width2()) / 2 , 0.103035)
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0334445 * (1 - exp(-0.621844 * (distance() + 0.13631)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0359918
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00568715 * (distance()) + 0.0719777) * (0.620372 - exp(-(0.0271884 * (distance()) + 0.477166) * (enclose() + -0.0330132 * (distance()) + 1.06244) - (0.0192708 * (distance()) + -0.00871356) * (enclose() + -0.447639 * (distance()) + 5.0831) * (enclose() + -0.447639 * (distance()) + 5.0831)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0334445 * (1 - exp(-0.621844 * (distance() + 0.13631)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0502067 * (0.620372 - exp(-0.613108 * (enclose() + 0.897374) - 0.0876404 * (enclose() + 2.8449) * (enclose() + 2.8449)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE INTRINSIC FRINGE metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.00613726 * (1 - exp(-0.563865 * (distance() + 0.166922)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.00659415
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.65218 - 1.27506 * (distance())) + 0.0137099 / pow(distance() , 1.06338)) * 1.41087 * pow((width1() + width2()) / 2 , 0.0938628)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE CROSSOVER FRINGE metal1 poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0467478 * (1 - exp(-0.964712 * (distance() + 0.0827855)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0519713
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0389327 * (distance()) + 1.71955) * (0.0438456 - exp(-(-0.296162 * (distance()) + 0.303378) * (enclose() + 4.67572 * (distance()) + 7.9499) - (0.00092346 * (distance()) + 0.0437379) * (enclose() + 5.05078 * (distance()) + 3.62104) * (enclose() + 5.05078 * (distance()) + 3.62104)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0467478 * (1 - exp(-0.964712 * (distance() + 0.0827855)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.957107 * (0.0438456 - exp(1.17743 * (enclose() + 31.3285) - 0.0483552 * (enclose() + 28.8749) * (enclose() + 28.8749)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.36302 - 0.564092 * (distance())) + 0.0164425 / pow(distance() , 0.891683)) * 1.86143 * pow((width1() + width2()) / 2 , 0.127802)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE CROSSOVER FRINGE metal2 poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0311522 * (1 - exp(-0.413794 * (distance() + 0.379007)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.036102
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0013877 * (distance()) + 0.0730198) * (0.593662 - exp(-(0.0333098 * (distance()) + 0.473226) * (enclose() + -0.0606838 * (distance()) + 1.38778) - (0.0218325 * (distance()) + -0.0934409) * (enclose() + -0.475358 * (distance()) + 1.64195) * (enclose() + -0.475358 * (distance()) + 1.64195)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0311522 * (1 - exp(-0.413794 * (distance() + 0.379007)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0399791 * (0.593662 - exp(-0.639775 * (enclose() + 1.08436) - 0.0157216 * (enclose() + -0.73484) * (enclose() + -0.73484)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE NEARBODY metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.67548 - 0.333718 * (distance())) + 0.0191313 / pow(distance() , 0.830325)) * 1.87439 * pow((width1() + width2()) / 2 , 0.141753)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE CROSSOVER FRINGE metal3 poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0244876 * (1 - exp(-0.328517 * (distance() + 0.645474)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0310367
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00141466 * (distance()) + 0.0418694) * (0.675921 - exp(-(0.0561642 * (distance()) + 0.185073) * (enclose() + -0.157558 * (distance()) + 1.89422) - (0.010036 * (distance()) + -0.0270826) * (enclose() + -0.418569 * (distance()) + 1.42986) * (enclose() + -0.418569 * (distance()) + 1.42986)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0244876 * (1 - exp(-0.328517 * (distance() + 0.645474)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0244713 * (0.675921 - exp(-0.465894 * (enclose() + 1.10643) - 0.0230974 * (enclose() + -0.662985) * (enclose() + -0.662985)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE NEARBODY metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.73794 - 0.215111 * (distance())) + 0.0212702 / pow(distance() , 0.809053)) * 1.78201 * pow((width1() + width2()) / 2 , 0.147814)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE CROSSOVER FRINGE metal4 poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.019682 * (1 - exp(-0.322828 * (distance() + 0.802819)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.028019
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00234259 * (distance()) + 0.0248428) * (0.673548 - exp(-(0.0302283 * (distance()) + 0.170526) * (enclose() + -0.0987613 * (distance()) + 2.0804) - (0.0100105 * (distance()) + -0.0131439) * (enclose() + -0.459571 * (distance()) + 2.29247) * (enclose() + -0.459571 * (distance()) + 2.29247)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.019682 * (1 - exp(-0.322828 * (distance() + 0.802819)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0182779 * (0.673548 - exp(-0.321667 * (enclose() + 1.58659) - 0.0369086 * (enclose() + -0.005385) * (enclose() + -0.005385)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE NEARBODY metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.60058 - 0.151674 * (distance())) + 0.0231962 / pow(distance() , 0.810173)) * 1.63557 * pow((width1() + width2()) / 2 , 0.152525)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE CROSSOVER FRINGE metal5 poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0160822 * (1 - exp(-0.347054 * (distance() + 0.882585)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0258587
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00048363 * (distance()) + 0.0335408) * (0.874384 - exp(-(0.027601 * (distance()) + 0.0598019) * (enclose() + -0.0912562 * (distance()) + 1.51905) - (0.00138779 * (distance()) + -0.00817385) * (enclose() + -0.723254 * (distance()) + 0.796651) * (enclose() + -0.723254 * (distance()) + 0.796651)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0160822 * (1 - exp(-0.347054 * (distance() + 0.882585)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0155613 * (0.874384 - exp(-0.197807 * (enclose() + 1.06277) - -0.0012349 * (enclose() + -2.81962) * (enclose() + -2.81962)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 15 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.69957 - 0.2445 * (distance())) + 0.0175376 / pow(distance() , 48.2692)) * 1.91499 * pow((width1() + width2()) / 2 , 0.0121844)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 poly MASK
[
    PROPERTY C
    max_caldistance = 15 // For reference
    max_calenclose = 15 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0384054 * (1 - exp(-0.0568993 * (distance() + 0.947585)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0299072
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00245601 * (distance()) + 0.186512) * (0.202205 - exp(-(-0.00966642 * (distance()) + 0.0560785) * (enclose() + 6.1152 * (distance()) + 25.795) - (8.82066e-07 * (distance()) + 0.00142295) * (enclose() + 6.30656 * (distance()) + -2.97566) * (enclose() + 6.30656 * (distance()) + -2.97566)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0384054 * (1 - exp(-0.0568993 * (distance() + 0.947585)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.111676 * (0.202205 - exp(0.0889178 * (enclose() + 117.523) - 0.00143618 * (enclose() + 91.6227) * (enclose() + 91.6227)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.90537 - 0.922127 * (distance())) + 0.0149826 / pow(distance() , 0.994772)) * 1.57541 * pow((width1() + width2()) / 2 , 0.109358)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0397484 * (1 - exp(-0.682267 * (distance() + 0.143653)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0447942
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0335587 * (distance()) + 0.715359) * (0.0779031 - exp(-(-0.131341 * (distance()) + 0.0966107) * (enclose() + 6.94602 * (distance()) + 14.1408) - (0.00031615 * (distance()) + 0.0236297) * (enclose() + 5.63367 * (distance()) + 6.53085) * (enclose() + 5.63367 * (distance()) + 6.53085)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0397484 * (1 - exp(-0.682267 * (distance() + 0.143653)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.441576 * (0.0779031 - exp(0.560094 * (enclose() + 48.8709) - 0.0252104 * (enclose() + 34.6992) * (enclose() + 34.6992)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.39509 - 0.483547 * (distance())) + 0.0190749 / pow(distance() , 0.86658)) * 1.71138 * pow((width1() + width2()) / 2 , 0.133002)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE CROSSOVER FRINGE metal3 metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0289824 * (1 - exp(-0.375962 * (distance() + 0.466731)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0343384
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00511409 * (distance()) + 0.0830267) * (0.969248 - exp(-(0.0665 * (distance()) + 0.304501) * (enclose() + -0.12691 * (distance()) + 1.57269) - (-0.0026019 * (distance()) + -0.0262125) * (enclose() + -0.309768 * (distance()) + 4.36844) * (enclose() + -0.309768 * (distance()) + 4.36844)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0289824 * (1 - exp(-0.375962 * (distance() + 0.466731)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0287281 * (0.969248 - exp(-0.637001 * (enclose() + 0.93814) - -0.039222 * (enclose() + 2.8196) * (enclose() + 2.8196)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.7149 - 0.285787 * (distance())) + 0.0200503 / pow(distance() , 0.821677)) * 1.83306 * pow((width1() + width2()) / 2 , 0.143894)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE CROSSOVER FRINGE metal4 metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.022816 * (1 - exp(-0.325059 * (distance() + 0.702797)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0300445
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00017954 * (distance()) + 0.0432662) * (0.884231 - exp(-(0.108199 * (distance()) + 0.157207) * (enclose() + 0.323619 * (distance()) + 1.26842) - (-0.00119466 * (distance()) + -0.0136012) * (enclose() + 1.22094 * (distance()) + 3.16785) * (enclose() + 1.22094 * (distance()) + 3.16785)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.022816 * (1 - exp(-0.325059 * (distance() + 0.702797)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0220819 * (0.884231 - exp(-0.698202 * (enclose() + 2.88652) - -0.0195745 * (enclose() + 9.27255) * (enclose() + 9.27255)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.65551 - 0.184222 * (distance())) + 0.0225086 / pow(distance() , 0.809667)) * 1.68377 * pow((width1() + width2()) / 2 , 0.150832)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE CROSSOVER FRINGE metal5 metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0181784 * (1 - exp(-0.336781 * (distance() + 0.829442)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0273429
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0004493 * (distance()) + 0.0417273) * (0.927842 - exp(-(0.0623944 * (distance()) + 0.1136) * (enclose() + 0.320186 * (distance()) + 1.55484) - (-0.00058631 * (distance()) + -0.0074658) * (enclose() + 1.1969 * (distance()) + 4.39107) * (enclose() + 1.1969 * (distance()) + 4.39107)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0181784 * (1 - exp(-0.336781 * (distance() + 0.829442)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0197404 * (0.927842 - exp(-0.425572 * (enclose() + 3.15577) - -0.0103973 * (enclose() + 10.3756) * (enclose() + 10.3756)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 15 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.68213 - 0.254596 * (distance())) + 0.0103356 / pow(distance() , 40.3392)) * 1.90363 * pow((width1() + width2()) / 2 , 0.0107929)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 metal1 MASK
[
    PROPERTY C
    max_caldistance = 15 // For reference
    max_calenclose = 15 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0386779 * (1 - exp(-0.0666441 * (distance() + 0.75041)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0316947
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00127346 * (distance()) + 0.0322458) * (0.953797 - exp(-(0.0208567 * (distance()) + -0.00688428) * (enclose() + -0.476479 * (distance()) + 5.37568) - (0.00125233 * (distance()) + -0.011446) * (enclose() + -0.446084 * (distance()) + -3.34845) * (enclose() + -0.446084 * (distance()) + -3.34845)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0386779 * (1 - exp(-0.0666441 * (distance() + 0.75041)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0256738 * (0.953797 - exp(-0.305966 * (enclose() + -1.7715) - 0.00733895 * (enclose() + -10.0397) * (enclose() + -10.0397)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.01388 - 0.926228 * (distance())) + 0.0137984 / pow(distance() , 0.989926)) * 1.72817 * pow((width1() + width2()) / 2 , 0.109189)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal3 metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0396343 * (1 - exp(-0.684535 * (distance() + 0.144672)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0447413
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0325114 * (distance()) + 0.578703) * (0.0931913 - exp(-(-0.11056 * (distance()) + 0.0685352) * (enclose() + 7.02097 * (distance()) + 14.858) - (0.00033814 * (distance()) + 0.0258198) * (enclose() + 4.9256 * (distance()) + 6.74774) * (enclose() + 4.9256 * (distance()) + 6.74774)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0396343 * (1 - exp(-0.684535 * (distance() + 0.144672)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.37063 * (0.0931913 - exp(0.484265 * (enclose() + 49.9629) - 0.0275105 * (enclose() + 31.3757) * (enclose() + 31.3757)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.57868 - 0.475294 * (distance())) + 0.0167341 / pow(distance() , 0.861659)) * 1.97808 * pow((width1() + width2()) / 2 , 0.133893)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal4 metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0285933 * (1 - exp(-0.383566 * (distance() + 0.461321)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0342779
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0049132 * (distance()) + 0.0401192) * (0.639664 - exp(-(0.00317191 * (distance()) + 0.201541) * (enclose() + -0.131708 * (distance()) + 1.45329) - (0.0153772 * (distance()) + -7.82364e-06) * (enclose() + -0.279994 * (distance()) + 4.48561) * (enclose() + -0.279994 * (distance()) + 4.48561)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0285933 * (1 - exp(-0.383566 * (distance() + 0.461321)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0323426 * (0.639664 - exp(-0.217401 * (enclose() + 0.79475) - 0.0768782 * (enclose() + 3.08564) * (enclose() + 3.08564)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.68112 - 0.26446 * (distance())) + 0.020495 / pow(distance() , 0.823557)) * 1.79042 * pow((width1() + width2()) / 2 , 0.146254)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal5 metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0222887 * (1 - exp(-0.332976 * (distance() + 0.707543)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0300029
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00084065 * (distance()) + 0.0395307) * (0.876647 - exp(-(0.117098 * (distance()) + 0.166887) * (enclose() + 0.382164 * (distance()) + 1.12214) - (-0.00128938 * (distance()) + -0.0149719) * (enclose() + 1.30823 * (distance()) + 2.79685) * (enclose() + 1.30823 * (distance()) + 2.79685)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0222887 * (1 - exp(-0.332976 * (distance() + 0.707543)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.021867 * (0.876647 - exp(-0.752377 * (enclose() + 3.03296) - -0.0214188 * (enclose() + 9.338) * (enclose() + 9.338)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 15 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.31097 - 0.196322 * (distance())) + 0.0744552 / pow(distance() , 1.17733)) * 1.28842 * pow((width1() + width2()) / 2 , 0.048956)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 metal2 MASK
[
    PROPERTY C
    max_caldistance = 15 // For reference
    max_calenclose = 15 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0394718 * (1 - exp(-0.0857587 * (distance() + 0.48675)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0347186
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0012394 * (distance()) + 0.0749309) * (0.661242 - exp(-(-0.00124174 * (distance()) + 0.064048) * (enclose() + 2.91267 * (distance()) + 3.96661) - (-0.00034142 * (distance()) + 2.08868e-05) * (enclose() + -0.00703521 * (distance()) + -17.4514) * (enclose() + -0.00703521 * (distance()) + -17.4514)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0394718 * (1 - exp(-0.0857587 * (distance() + 0.48675)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0467609 * (0.661242 - exp(-0.0454219 * (enclose() + 47.6567) - -0.00510041 * (enclose() + -17.5569) * (enclose() + -17.5569)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.03357 - 0.926471 * (distance())) + 0.01418 / pow(distance() , 0.983665)) * 1.71183 * pow((width1() + width2()) / 2 , 0.110212)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal4 metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0394925 * (1 - exp(-0.685512 * (distance() + 0.146496)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0447264
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0115768 * (distance()) + 0.188087) * (0.280635 - exp(-(-0.0795027 * (distance()) + 0.0649281) * (enclose() + 4.539 * (distance()) + 6.30701) - (0.00165662 * (distance()) + 0.0478928) * (enclose() + 2.10209 * (distance()) + 3.77004) * (enclose() + 2.10209 * (distance()) + 3.77004)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0394925 * (1 - exp(-0.685512 * (distance() + 0.146496)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.122985 * (0.280635 - exp(0.332585 * (enclose() + 29.002) - 0.0561759 * (enclose() + 14.2805) * (enclose() + 14.2805)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.65225 - 0.453718 * (distance())) + 0.0169349 / pow(distance() , 0.852173)) * 2.00391 * pow((width1() + width2()) / 2 , 0.13721)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal5 metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0282457 * (1 - exp(-0.385518 * (distance() + 0.47371)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0343032
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00378076 * (distance()) + 0.0442611) * (0.665691 - exp(-(0.0647 * (distance()) + 0.218425) * (enclose() + -0.200485 * (distance()) + 1.71398) - (0.00463993 * (distance()) + -0.00267195) * (enclose() + -0.223141 * (distance()) + 6.10129) * (enclose() + -0.223141 * (distance()) + 6.10129)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0282457 * (1 - exp(-0.385518 * (distance() + 0.47371)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0315824 * (0.665691 - exp(-0.541925 * (enclose() + 0.711555) - 0.0205277 * (enclose() + 4.98558) * (enclose() + 4.98558)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 15 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.48993 - 0.220174 * (distance())) + 0.0528779 / pow(distance() , 1.2626)) * 1.72819 * pow((width1() + width2()) / 2 , 0.0427186)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 metal3 MASK
[
    PROPERTY C
    max_caldistance = 15 // For reference
    max_calenclose = 15 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0416209 * (1 - exp(-0.11427 * (distance() + 0.271521)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0389534
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0060822 * (distance()) + 0.239297) * (0.565138 - exp(-(0.0238111 * (distance()) + 0.147716) * (enclose() + 0.22702 * (distance()) + 5.5696) - (7.9821e-05 * (distance()) + -0.0078418) * (enclose() + 1.14048 * (distance()) + 6.51212) * (enclose() + 1.14048 * (distance()) + 6.51212)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0416209 * (1 - exp(-0.11427 * (distance() + 0.271521)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.074032 * (0.565138 - exp(-0.504883 * (enclose() + 8.9749) - -0.00664448 * (enclose() + 23.6193) * (enclose() + 23.6193)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.14561 - 0.918281 * (distance())) + 0.0141025 / pow(distance() , 0.968079)) * 1.79139 * pow((width1() + width2()) / 2 , 0.11275)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal5 metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.039312 * (1 - exp(-0.685587 * (distance() + 0.151531)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.044846
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0125051 * (distance()) + 0.193636) * (0.270542 - exp(-(-0.0734248 * (distance()) + 0.0645309) * (enclose() + 4.69663 * (distance()) + 6.58274) - (0.00186756 * (distance()) + 0.0482895) * (enclose() + 1.99585 * (distance()) + 3.81033) * (enclose() + 1.99585 * (distance()) + 3.81033)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.039312 * (1 - exp(-0.685587 * (distance() + 0.151531)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.128081 * (0.270542 - exp(0.302593 * (enclose() + 30.0659) - 0.0576273 * (enclose() + 13.7896) * (enclose() + 13.7896)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 15 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.44753 - 0.25454 * (distance())) + 0.046466 / pow(distance() , 1.38244)) * 1.84011 * pow((width1() + width2()) / 2 , 0.0396423)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 metal4 MASK
[
    PROPERTY C
    max_caldistance = 15 // For reference
    max_calenclose = 15 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0463004 * (1 - exp(-0.163519 * (distance() + 0.133308)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0458305
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00714551 * (distance()) + 0.302014) * (0.465544 - exp(-(0.0367445 * (distance()) + 0.208006) * (enclose() + 0.179999 * (distance()) + 4.81069) - (0.00018819 * (distance()) + -0.0139908) * (enclose() + 1.03155 * (distance()) + 4.77753) * (enclose() + 1.03155 * (distance()) + 4.77753)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0463004 * (1 - exp(-0.163519 * (distance() + 0.133308)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0974157 * (0.465544 - exp(-0.759174 * (enclose() + 7.51067) - -0.0111679 * (enclose() + 20.2508) * (enclose() + 20.2508)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 15 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.14516 - 0.31119 * (distance())) + 0.0534605 / pow(distance() , 1.54703)) * 1.43455 * pow((width1() + width2()) / 2 , 0.0457125)
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 metal5 MASK
[
    PROPERTY C
    max_caldistance = 15 // For reference
    max_calenclose = 15 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0600438 * (1 - exp(-0.253068 * (distance() + 0.323103)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0614775
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0032703 * (distance()) + 0.198103) * (0.776814 - exp(-(0.0751408 * (distance()) + 0.170162) * (enclose() + 0.183758 * (distance()) + 1.34438) - (0.00045096 * (distance()) + -0.0318982) * (enclose() + 0.887587 * (distance()) + 0.225222) * (enclose() + 0.887587 * (distance()) + 0.225222)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0600438 * (1 - exp(-0.253068 * (distance() + 0.323103)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0745243 * (0.776814 - exp(-1.29727 * (enclose() + 4.10075) - -0.0251338 * (enclose() + 13.539) * (enclose() + 13.539)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal2
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.52719 - 2.19627 * (distance())) + 7.4247e-09 / pow(distance() , 9.50379)) * 1.86987 * pow((width1() + width2()) / 2 , 0.0399371)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal2
//
CAPACITANCE CROSSOVER FRINGE metal1 metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0212215 * (1 - exp(-1.28443 * (distance() + -0.0238295)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.021812
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00112414 * (distance()) + 0.114154) * (0.374764 - exp(-(-0.0295547 * (distance()) + -0.0235552) * (enclose() + 25.2112 * (distance()) + 0.766893) - (0.00163998 * (distance()) + 0.0626541) * (enclose() + 2.91807 * (distance()) + 3.58184) * (enclose() + 2.91807 * (distance()) + 3.58184)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0212215 * (1 - exp(-1.28443 * (distance() + -0.0238295)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0542666 * (0.374764 - exp(0.171329 * (enclose() + 126.823) - 0.070854 * (enclose() + 18.1722) * (enclose() + 18.1722)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal2
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0326406 * (1 - exp(-1.37845 * (distance() + -0.0203826)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0335435
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00260207 * (distance()) + 0.25292) * (0.26372 - exp(-(-0.323054 * (distance()) + 0.276012) * (enclose() + 2.819 * (distance()) + 2.85501) - (0.0063472 * (distance()) + 0.119295) * (enclose() + 2.18742 * (distance()) + 1.77353) * (enclose() + 2.18742 * (distance()) + 1.77353)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0326406 * (1 - exp(-1.37845 * (distance() + -0.0203826)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.119955 * (0.26372 - exp(1.33926 * (enclose() + 16.95) - 0.151031 * (enclose() + 12.7106) * (enclose() + 12.7106)) + 2.39682e-06)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal3
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.06249 - 1.42138 * (distance())) + 0.00589662 / pow(distance() , 1.39082)) * 1.44489 * pow((width1() + width2()) / 2 , 0.0688586)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal3
//
CAPACITANCE CROSSOVER FRINGE metal1 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0111788 * (1 - exp(-0.918331 * (distance() + 0.0393019)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0116281
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0004901 * (distance()) + 0.0310469) * (0.724687 - exp(-(-0.00736888 * (distance()) + 0.27145) * (enclose() + -2.45223 * (distance()) + 2.33683) - (0.0155642 * (distance()) + -0.00718055) * (enclose() + -0.19108 * (distance()) + 7.77907) * (enclose() + -0.19108 * (distance()) + 7.77907)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0111788 * (1 - exp(-0.918331 * (distance() + 0.0393019)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0142982 * (0.724687 - exp(-0.234606 * (enclose() + -9.92432) - 0.0706405 * (enclose() + 6.82367) * (enclose() + 6.82367)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal3
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0385487 * (1 - exp(-1.0713 * (distance() + 0.031206)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0400951
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00527122 * (distance()) + 1.68994) * (0.0435383 - exp(-(-0.278966 * (distance()) + 0.222377) * (enclose() + 5.67045 * (distance()) + 9.59221) - (0.00090282 * (distance()) + 0.0458552) * (enclose() + 5.30795 * (distance()) + 4.31631) * (enclose() + 5.30795 * (distance()) + 4.31631)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0385487 * (1 - exp(-1.0713 * (distance() + 0.031206)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.858148 * (0.0435383 - exp(1.17245 * (enclose() + 37.9445) - 0.0503693 * (enclose() + 30.8561) * (enclose() + 30.8561)) + 0.000871052)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal4
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.31773 - 1.28068 * (distance())) + 0.00776554 / pow(distance() , 1.26529)) * 1.5234 * pow((width1() + width2()) / 2 , 0.0808907)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal4
//
CAPACITANCE CROSSOVER FRINGE metal1 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00763605 * (1 - exp(-0.844972 * (distance() + 0.0713361)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00808354
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00051067 * (distance()) + 0.0204396) * (0.853691 - exp(-(0.210419 * (distance()) + 0.162649) * (enclose() + 0.172315 * (distance()) + 0.815358) - (-0.00676166 * (distance()) + -0.0154633) * (enclose() + 0.69748 * (distance()) + 1.8972) * (enclose() + 0.69748 * (distance()) + 1.8972)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00763605 * (1 - exp(-0.844972 * (distance() + 0.0713361)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00894312 * (0.853691 - exp(-1.21474 * (enclose() + 1.67693) - -0.0492716 * (enclose() + 5.3846) * (enclose() + 5.3846)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal4
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.040886 * (1 - exp(-1.00228 * (distance() + 0.0541867)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.043207
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-6.99293e-05 * (distance()) + 0.134611) * (0.606651 - exp(-(-0.066898 * (distance()) + 1.33136) * (enclose() + 0.145533 * (distance()) + 0.472176) - (0.0935338 * (distance()) + -0.560307) * (enclose() + -0.51106 * (distance()) + 0.67184) * (enclose() + -0.51106 * (distance()) + 0.67184)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.040886 * (1 - exp(-1.00228 * (distance() + 0.0541867)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0671307 * (0.606651 - exp(-0.99687 * (enclose() + 1.19984) - -0.092638 * (enclose() + -1.88346) * (enclose() + -1.88346)) + 0.0014519)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal5
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.43335 - 1.23597 * (distance())) + 0.00929423 / pow(distance() , 1.19856)) * 1.50689 * pow((width1() + width2()) / 2 , 0.0860048)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal5
//
CAPACITANCE CROSSOVER FRINGE metal1 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00579503 * (1 - exp(-0.82378 * (distance() + 0.0858889)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00622675
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0005792 * (distance()) + 0.0184366) * (0.672698 - exp(-(0.0590628 * (distance()) + 0.178852) * (enclose() + -0.098853 * (distance()) + 2.11464) - (0.00819183 * (distance()) + -0.0167872) * (enclose() + -0.0157003 * (distance()) + 1.9958) * (enclose() + -0.0157003 * (distance()) + 1.9958)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00579503 * (1 - exp(-0.82378 * (distance() + 0.0858889)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0077703 * (0.672698 - exp(-0.474166 * (enclose() + 1.62038) - 0.024172 * (enclose() + 1.9173) * (enclose() + 1.9173)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal5
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.042109 * (1 - exp(-0.980898 * (distance() + 0.0641258)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0450602
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00048049 * (distance()) + 0.142143) * (0.577543 - exp(-(-0.0722791 * (distance()) + 1.34251) * (enclose() + 0.140608 * (distance()) + 0.513873) - (0.0946783 * (distance()) + -0.556901) * (enclose() + -0.505949 * (distance()) + 0.688443) * (enclose() + -0.505949 * (distance()) + 0.688443)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.042109 * (1 - exp(-0.980898 * (distance() + 0.0641258)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0722727 * (0.577543 - exp(-0.981115 * (enclose() + 1.21691) - -0.0835095 * (enclose() + -1.8413) * (enclose() + -1.8413)) + 0.00188202)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal6
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.55769 - 1.22442 * (distance())) + 0.00985847 / pow(distance() , 1.15836)) * 1.5688 * pow((width1() + width2()) / 2 , 0.0884375)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal6
//
CAPACITANCE CROSSOVER FRINGE metal1 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00458475 * (1 - exp(-0.819002 * (distance() + 0.0925834)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00497531
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00089277 * (distance()) + 0.0146847) * (0.864265 - exp(-(0.0525367 * (distance()) + 0.0826322) * (enclose() + 0.119829 * (distance()) + 1.08809) - (0.00170637 * (distance()) + -0.0119369) * (enclose() + -0.415631 * (distance()) + 0.771675) * (enclose() + -0.415631 * (distance()) + 0.771675)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00458475 * (1 - exp(-0.819002 * (distance() + 0.0925834)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00511043 * (0.864265 - exp(-0.345316 * (enclose() + 1.68724) - -0.00340505 * (enclose() + -1.30648) * (enclose() + -1.30648)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal6
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.042946 * (1 - exp(-0.973896 * (distance() + 0.0688483)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0463388
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00077456 * (distance()) + 0.138891) * (0.593336 - exp(-(-0.0751148 * (distance()) + 1.33141) * (enclose() + 0.143449 * (distance()) + 0.495396) - (0.094117 * (distance()) + -0.552113) * (enclose() + -0.506411 * (distance()) + 0.686277) * (enclose() + -0.506411 * (distance()) + 0.686277)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.042946 * (1 - exp(-0.973896 * (distance() + 0.0688483)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0713819 * (0.593336 - exp(-0.955836 * (enclose() + 1.21264) - -0.081528 * (enclose() + -1.84578) * (enclose() + -1.84578)) + 0.00219124)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal2,metal3
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.90361 - 1.18712 * (distance())) + 0.00663452 / pow(distance() , 1.43909)) * 1.2597 * pow((width1() + width2()) / 2 , 0.0707869)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal2 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0293444 * (1 - exp(-0.822252 * (distance() + 0.0312796)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0300973
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00248777 * (distance()) + 0.156153) * (0.390487 - exp(-(0.0282241 * (distance()) + 1.0566) * (enclose() + 0.0711632 * (distance()) + 1.11862) - (0.0434335 * (distance()) + -0.329537) * (enclose() + -0.438887 * (distance()) + 1.0589) * (enclose() + -0.438887 * (distance()) + 1.0589)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0293444 * (1 - exp(-0.822252 * (distance() + 0.0312796)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0718571 * (0.390487 - exp(-1.19772 * (enclose() + 1.47444) - -0.11237 * (enclose() + -1.13554) * (enclose() + -1.13554)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal2 poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0154725 * (1 - exp(-0.759964 * (distance() + 0.0390129)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0158294
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00019582 * (distance()) + 0.0561994) * (0.509458 - exp(-(0.055098 * (distance()) + 0.567356) * (enclose() + -0.0738269 * (distance()) + 1.40562) - (0.0588147 * (distance()) + -0.111967) * (enclose() + -0.399434 * (distance()) + 1.64885) * (enclose() + -0.399434 * (distance()) + 1.64885)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0154725 * (1 - exp(-0.759964 * (distance() + 0.0390129)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0285892 * (0.509458 - exp(-0.842846 * (enclose() + 1.03649) - 0.182107 * (enclose() + -0.34832) * (enclose() + -0.34832)) + 0.000920308)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal2,metal4
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.96061 - 0.826597 * (distance())) + 0.0119428 / pow(distance() , 1.26027)) * 1.09501 * pow((width1() + width2()) / 2 , 0.0993696)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal2 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0180067 * (1 - exp(-0.48885 * (distance() + 0.181172)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0180609
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00011012 * (distance()) + 0.0382443) * (0.863608 - exp(-(0.304211 * (distance()) + 0.287018) * (enclose() + 0.277931 * (distance()) + 0.776983) - (-0.00619325 * (distance()) + -0.0441986) * (enclose() + 0.956268 * (distance()) + 1.80479) * (enclose() + 0.956268 * (distance()) + 1.80479)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0180067 * (1 - exp(-0.48885 * (distance() + 0.181172)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0193975 * (0.863608 - exp(-1.80807 * (enclose() + 2.16664) - -0.0751649 * (enclose() + 6.58613) * (enclose() + 6.58613)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal2 poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0212047 * (1 - exp(-0.49418 * (distance() + 0.173204)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0213027
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00055099 * (distance()) + 0.0706793) * (0.514866 - exp(-(0.043396 * (distance()) + 0.536704) * (enclose() + -0.0757085 * (distance()) + 1.50909) - (0.0368671 * (distance()) + -0.110648) * (enclose() + -0.403191 * (distance()) + 1.60867) * (enclose() + -0.403191 * (distance()) + 1.60867)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0212047 * (1 - exp(-0.49418 * (distance() + 0.173204)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0367171 * (0.514866 - exp(-0.753684 * (enclose() + 1.13055) - 0.0736875 * (enclose() + -0.407285) * (enclose() + -0.407285)) + 0.00231725)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal2,metal5
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.38646 - 0.702636 * (distance())) + 0.012216 / pow(distance() , 1.14366)) * 1.40321 * pow((width1() + width2()) / 2 , 0.110661)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal2 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0131218 * (1 - exp(-0.41506 * (distance() + 0.26608)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0132933
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00030886 * (distance()) + 0.0286743) * (0.92231 - exp(-(0.194061 * (distance()) + 0.238533) * (enclose() + 0.434087 * (distance()) + 0.985392) - (-0.00188424 * (distance()) + -0.0278819) * (enclose() + 1.3484 * (distance()) + 2.67675) * (enclose() + 1.3484 * (distance()) + 2.67675)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0131218 * (1 - exp(-0.41506 * (distance() + 0.26608)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.013565 * (0.92231 - exp(-1.20884 * (enclose() + 3.15583) - -0.0373031 * (enclose() + 9.41875) * (enclose() + 9.41875)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal2 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0239344 * (1 - exp(-0.429293 * (distance() + 0.247421)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0243361
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0002647 * (distance()) + 0.07202) * (0.570004 - exp(-(0.045827 * (distance()) + 0.485437) * (enclose() + -0.0774161 * (distance()) + 1.4224) - (0.0253954 * (distance()) + -0.099726) * (enclose() + -0.445891 * (distance()) + 1.59054) * (enclose() + -0.445891 * (distance()) + 1.59054)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0239344 * (1 - exp(-0.429293 * (distance() + 0.247421)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0366718 * (0.570004 - exp(-0.714572 * (enclose() + 1.03532) - 0.027251 * (enclose() + -0.638915) * (enclose() + -0.638915)) + 0.00359191)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal2,metal6
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.56614 - 0.644061 * (distance())) + 0.01418 / pow(distance() , 1.06528)) * 1.4445 * pow((width1() + width2()) / 2 , 0.117054)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal2 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0100219 * (1 - exp(-0.394237 * (distance() + 0.31895)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.010419
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00076864 * (distance()) + 0.024183) * (0.99579 - exp(-(0.108434 * (distance()) + 0.180584) * (enclose() + 0.577044 * (distance()) + 1.46598) - (-0.00065551 * (distance()) + -0.0128173) * (enclose() + 1.64452 * (distance()) + 4.81143) * (enclose() + 1.64452 * (distance()) + 4.81143)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0100219 * (1 - exp(-0.394237 * (distance() + 0.31895)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0101699 * (0.99579 - exp(-0.722754 * (enclose() + 4.3512) - -0.0160949 * (enclose() + 13.034) * (enclose() + 13.034)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal2 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0254146 * (1 - exp(-0.410992 * (distance() + 0.292469)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0265077
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0008096 * (distance()) + 0.0717081) * (0.575369 - exp(-(0.0406747 * (distance()) + 0.488252) * (enclose() + -0.0714634 * (distance()) + 1.41136) - (0.0252413 * (distance()) + -0.0986128) * (enclose() + -0.450455 * (distance()) + 1.62148) * (enclose() + -0.450455 * (distance()) + 1.62148)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0254146 * (1 - exp(-0.410992 * (distance() + 0.292469)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.037878 * (0.575369 - exp(-0.691626 * (enclose() + 1.05404) - 0.0275937 * (enclose() + -0.630795) * (enclose() + -0.630795)) + 0.00458252)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal3,metal4
//
CAPACITANCE NEARBODY metal3 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.98116 - 1.00443 * (distance())) + 0.0113624 / pow(distance() , 1.27492)) * 1.09568 * pow((width1() + width2()) / 2 , 0.0872447)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0324893 * (1 - exp(-0.72284 * (distance() + 0.0776569)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0338347
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00976942 * (distance()) + 0.329535) * (0.159151 - exp(-(-0.00712526 * (distance()) + -0.0017736) * (enclose() + 32.0295 * (distance()) + 56.1186) - (0.00821084 * (distance()) + 0.0212516) * (enclose() + 0.765321 * (distance()) + 9.18581) * (enclose() + 0.765321 * (distance()) + 9.18581)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0324893 * (1 - exp(-0.72284 * (distance() + 0.0776569)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.189191 * (0.159151 - exp(0.0373999 * (enclose() + 216.266) - 0.0623058 * (enclose() + 13.0124) * (enclose() + 13.0124)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0103916 * (1 - exp(-0.650019 * (distance() + 0.0971874)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0107802
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00106644 * (distance()) + 0.0312029) * (0.737743 - exp(-(0.100284 * (distance()) + 0.157677) * (enclose() + -0.155289 * (distance()) + 1.35407) - (0.0133678 * (distance()) + -0.0284486) * (enclose() + -0.445406 * (distance()) + 1.1086) * (enclose() + -0.445406 * (distance()) + 1.1086)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0103916 * (1 - exp(-0.650019 * (distance() + 0.0971874)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0129353 * (0.737743 - exp(-0.659097 * (enclose() + 0.577625) - 0.0383904 * (enclose() + -1.11843) * (enclose() + -1.11843)) + 0.00175933)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal3,metal5
//
CAPACITANCE NEARBODY metal3 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.35906 - 0.673281 * (distance())) + 0.0128846 / pow(distance() , 1.13533)) * 1.35624 * pow((width1() + width2()) / 2 , 0.113502)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0213122 * (1 - exp(-0.395898 * (distance() + 0.288808)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0214519
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00081744 * (distance()) + 0.0401486) * (0.857093 - exp(-(0.282082 * (distance()) + 0.344526) * (enclose() + 0.291098 * (distance()) + 0.973589) - (-0.00497037 * (distance()) + -0.0434042) * (enclose() + 0.979505 * (distance()) + 2.37307) * (enclose() + 0.979505 * (distance()) + 2.37307)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0213122 * (1 - exp(-0.395898 * (distance() + 0.288808)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0221179 * (0.857093 - exp(-1.75494 * (enclose() + 2.42908) - -0.068256 * (enclose() + 7.2706) * (enclose() + 7.2706)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0152635 * (1 - exp(-0.388676 * (distance() + 0.292585)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0153129
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00242824 * (distance()) + 0.0456325) * (0.844012 - exp(-(0.0627894 * (distance()) + 0.11655) * (enclose() + -0.0540838 * (distance()) + 1.03384) - (0.00179611 * (distance()) + -0.0236578) * (enclose() + -0.555071 * (distance()) + 1.01013) * (enclose() + -0.555071 * (distance()) + 1.01013)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0152635 * (1 - exp(-0.388676 * (distance() + 0.292585)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0167457 * (0.844012 - exp(-0.430497 * (enclose() + 0.763421) - -0.0146772 * (enclose() + -1.76522) * (enclose() + -1.76522)) + 0.00384124)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal3,metal6
//
CAPACITANCE NEARBODY metal3 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.52829 - 0.545161 * (distance())) + 0.0164956 / pow(distance() , 1.04005)) * 1.32898 * pow((width1() + width2()) / 2 , 0.125573)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0157074 * (1 - exp(-0.320108 * (distance() + 0.442449)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0159814
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00037329 * (distance()) + 0.028047) * (0.941145 - exp(-(0.153624 * (distance()) + 0.198119) * (enclose() + 0.509742 * (distance()) + 0.93104) - (-0.00168927 * (distance()) + -0.0183812) * (enclose() + 1.44036 * (distance()) + 3.02313) * (enclose() + 1.44036 * (distance()) + 3.02313)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0157074 * (1 - exp(-0.320108 * (distance() + 0.442449)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0149567 * (0.941145 - exp(-0.966239 * (enclose() + 3.47975) - -0.0268275 * (enclose() + 10.2249) * (enclose() + 10.2249)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0180179 * (1 - exp(-0.321606 * (distance() + 0.432805)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0183266
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00257973 * (distance()) + 0.0512351) * (0.857708 - exp(-(0.0501276 * (distance()) + 0.105454) * (enclose() + -0.0084995 * (distance()) + 1.02313) - (0.00124198 * (distance()) + -0.023278) * (enclose() + -0.619789 * (distance()) + 0.876174) * (enclose() + -0.619789 * (distance()) + 0.876174)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0180179 * (1 - exp(-0.321606 * (distance() + 0.432805)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0191682 * (0.857708 - exp(-0.356092 * (enclose() + 0.980633) - -0.0170681 * (enclose() + -2.22277) * (enclose() + -2.22277)) + 0.00568109)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal4,metal5
//
CAPACITANCE NEARBODY metal4 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.26469 - 0.93631 * (distance())) + 0.0122168 / pow(distance() , 1.184)) * 1.25721 * pow((width1() + width2()) / 2 , 0.0947247)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0340448 * (1 - exp(-0.69405 * (distance() + 0.0986587)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0361105
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0002451 * (distance()) + 0.0935649) * (0.697069 - exp(-(0.00561082 * (distance()) + 0.971829) * (enclose() + 0.113238 * (distance()) + 0.580115) - (0.0410156 * (distance()) + -0.304267) * (enclose() + -0.437077 * (distance()) + 1.02462) * (enclose() + -0.437077 * (distance()) + 1.02462)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0340448 * (1 - exp(-0.69405 * (distance() + 0.0986587)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0461697 * (0.697069 - exp(-0.999883 * (enclose() + 1.1463) - -0.099189 * (enclose() + -1.16077) * (enclose() + -1.16077)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0078056 * (1 - exp(-0.620061 * (distance() + 0.123745)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0082582
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00134854 * (distance()) + 0.0242707) * (0.829953 - exp(-(0.0710664 * (distance()) + 0.0909269) * (enclose() + -0.118309 * (distance()) + 1.13745) - (0.00529656 * (distance()) + -0.0144798) * (enclose() + -0.459015 * (distance()) + 1.05417) * (enclose() + -0.459015 * (distance()) + 1.05417)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0078056 * (1 - exp(-0.620061 * (distance() + 0.123745)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.008764 * (0.829953 - exp(-0.446259 * (enclose() + 0.545905) - 0.012003 * (enclose() + -1.24091) * (enclose() + -1.24091)) + 0.00241945)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal4,metal6
//
CAPACITANCE NEARBODY metal4 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.5588 - 0.587083 * (distance())) + 0.0152971 / pow(distance() , 1.04956)) * 1.39532 * pow((width1() + width2()) / 2 , 0.12181)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0223204 * (1 - exp(-0.358404 * (distance() + 0.371462)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0229867
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00188859 * (distance()) + 0.0349574) * (0.859929 - exp(-(0.241586 * (distance()) + 0.232557) * (enclose() + 0.348926 * (distance()) + 0.728704) - (-0.00531615 * (distance()) + -0.025406) * (enclose() + 1.09682 * (distance()) + 1.84474) * (enclose() + 1.09682 * (distance()) + 1.84474)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0223204 * (1 - exp(-0.358404 * (distance() + 0.371462)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0222002 * (0.859929 - exp(-1.44049 * (enclose() + 2.47333) - -0.0519867 * (enclose() + 7.32884) * (enclose() + 7.32884)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.01208 * (1 - exp(-0.349877 * (distance() + 0.382318)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0124035
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00226498 * (distance()) + 0.0373457) * (0.899707 - exp(-(0.0456207 * (distance()) + 0.0642481) * (enclose() + -0.0346744 * (distance()) + 0.90168) - (0.00065073 * (distance()) + -0.0116098) * (enclose() + -0.550535 * (distance()) + 0.914462) * (enclose() + -0.550535 * (distance()) + 0.914462)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.01208 * (1 - exp(-0.349877 * (distance() + 0.382318)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0130104 * (0.899707 - exp(-0.292352 * (enclose() + 0.728308) - -0.00835615 * (enclose() + -1.83821) * (enclose() + -1.83821)) + 0.00518232)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.37425 - 0.863874 * (distance())) + 0.0144558 / pow(distance() , 1.10713)) * 1.25821 * pow((width1() + width2()) / 2 , 0.101654)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0331436 * (1 - exp(-0.622939 * (distance() + 0.133582)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0354915
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00017336 * (distance()) + 0.0694733) * (0.891435 - exp(-(0.0905471 * (distance()) + 1.3237) * (enclose() + -0.191601 * (distance()) + 1.94757) - (-0.00026081 * (distance()) + -0.103585) * (enclose() + -0.41824 * (distance()) + 4.98061) * (enclose() + -0.41824 * (distance()) + 4.98061)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0331436 * (1 - exp(-0.622939 * (distance() + 0.133582)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0343033 * (0.891435 - exp(-1.77644 * (enclose() + 0.989565) - -0.104889 * (enclose() + 2.88941) * (enclose() + 2.88941)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00664508 * (1 - exp(-0.564193 * (distance() + 0.16379)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00709844
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00153586 * (distance()) + 0.0223853) * (0.88178 - exp(-(0.0492559 * (distance()) + 0.0605388) * (enclose() + -0.0847516 * (distance()) + 0.972794) - (0.00250755 * (distance()) + -0.00992348) * (enclose() + -0.466741 * (distance()) + 0.999544) * (enclose() + -0.466741 * (distance()) + 0.999544)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00664508 * (1 - exp(-0.564193 * (distance() + 0.16379)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.007353 * (0.88178 - exp(-0.306818 * (enclose() + 0.549036) - 0.00261427 * (enclose() + -1.33416) * (enclose() + -1.33416)) + 0.00319376)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal2,metal3
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.80613 - 1.54975 * (distance())) + 0.00348371 / pow(distance() , 1.66487)) * 1.39287 * pow((width1() + width2()) / 2 , 0.0452943)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal2 metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0245125 * (1 - exp(-1.0753 * (distance() + -0.0261218)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0250101
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00062325 * (distance()) + 0.126978) * (0.379735 - exp(-(-0.0489811 * (distance()) + 0.0492667) * (enclose() + 5.0666 * (distance()) + 2.43026) - (0.0240307 * (distance()) + 0.0578212) * (enclose() + 0.585661 * (distance()) + 3.37667) * (enclose() + 0.585661 * (distance()) + 3.37667)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0245125 * (1 - exp(-1.0753 * (distance() + -0.0261218)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0619309 * (0.379735 - exp(0.195639 * (enclose() + 27.7633) - 0.177975 * (enclose() + 6.30497) * (enclose() + 6.30497)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0247704 * (1 - exp(-1.06948 * (distance() + -0.0263717)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0252787
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00212287 * (distance()) + 0.290439) * (0.171484 - exp(-(-0.113933 * (distance()) + 0.0460802) * (enclose() + 8.82894 * (distance()) + 13.0333) - (0.00072879 * (distance()) + 0.0385972) * (enclose() + 4.5306 * (distance()) + 4.96215) * (enclose() + 4.5306 * (distance()) + 4.96215)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0247704 * (1 - exp(-1.06948 * (distance() + -0.0263717)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.139912 * (0.171484 - exp(0.523585 * (enclose() + 57.178) - 0.0422411 * (enclose() + 27.6151) * (enclose() + 27.6151)) + 0.000348481)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal2,metal4
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.93467 - 1.11597 * (distance())) + 0.00774583 / pow(distance() , 1.39185)) * 1.21878 * pow((width1() + width2()) / 2 , 0.0773335)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal2 metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0135969 * (1 - exp(-0.715094 * (distance() + 0.0641138)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0139182
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00042681 * (distance()) + 0.0342589) * (0.779041 - exp(-(0.178997 * (distance()) + 0.159064) * (enclose() + 0.219985 * (distance()) + 0.835556) - (-0.00018912 * (distance()) + -0.00050802) * (enclose() + -6.22028 * (distance()) + 2.06149) * (enclose() + -6.22028 * (distance()) + 2.06149)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0135969 * (1 - exp(-0.715094 * (distance() + 0.0641138)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0160624 * (0.779041 - exp(-1.05405 * (enclose() + 1.93548) - -0.00145362 * (enclose() + -29.0399) * (enclose() + -29.0399)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0307207 * (1 - exp(-0.778359 * (distance() + 0.0489958)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0315374
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00539315 * (distance()) + 0.375308) * (0.146271 - exp(-(-0.0711024 * (distance()) + 0.026895) * (enclose() + 10.5276 * (distance()) + 19.5721) - (0.00049558 * (distance()) + 0.0274105) * (enclose() + 4.58234 * (distance()) + 6.65012) * (enclose() + 4.58234 * (distance()) + 6.65012)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0307207 * (1 - exp(-0.778359 * (distance() + 0.0489958)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.201137 * (0.146271 - exp(0.328617 * (enclose() + 72.2101) - 0.0298884 * (enclose() + 29.5618) * (enclose() + 29.5618)) + 0.0011322)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal2,metal5
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.98403 - 0.986565 * (distance())) + 0.0126021 / pow(distance() , 1.2427)) * 1.05625 * pow((width1() + width2()) / 2 , 0.0906263)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal2 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00948669 * (1 - exp(-0.64155 * (distance() + 0.111173)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00990575
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00039072 * (distance()) + 0.0236131) * (0.869892 - exp(-(0.209051 * (distance()) + 0.16761) * (enclose() + 0.277933 * (distance()) + 0.71993) - (-0.00460023 * (distance()) + -0.0205094) * (enclose() + 0.979411 * (distance()) + 1.84659) * (enclose() + 0.979411 * (distance()) + 1.84659)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00948669 * (1 - exp(-0.64155 * (distance() + 0.111173)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0108298 * (0.869892 - exp(-1.21287 * (enclose() + 2.10959) - -0.0435106 * (enclose() + 6.74364) * (enclose() + 6.74364)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0332378 * (1 - exp(-0.712251 * (distance() + 0.0851299)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0347909
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00930505 * (distance()) + 0.343677) * (0.161409 - exp(-(-0.0871883 * (distance()) + 0.047736) * (enclose() + 7.44646 * (distance()) + 13.6272) - (0.00061775 * (distance()) + 0.0302767) * (enclose() + 3.99192 * (distance()) + 5.72681) * (enclose() + 3.99192 * (distance()) + 5.72681)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0332378 * (1 - exp(-0.712251 * (distance() + 0.0851299)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.195101 * (0.161409 - exp(0.388205 * (enclose() + 50.8595) - 0.0333654 * (enclose() + 25.6864) * (enclose() + 25.6864)) + 0.00194818)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal2,metal6
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.32075 - 0.928217 * (distance())) + 0.0126003 / pow(distance() , 1.16131)) * 1.28134 * pow((width1() + width2()) / 2 , 0.0978151)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal2 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0071083 * (1 - exp(-0.618786 * (distance() + 0.137655)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00755559
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00042098 * (distance()) + 0.0181774) * (0.919661 - exp(-(0.124323 * (distance()) + 0.119029) * (enclose() + 0.416776 * (distance()) + 0.884443) - (-0.00240448 * (distance()) + -0.00708076) * (enclose() + 1.12856 * (distance()) + 3.75054) * (enclose() + 1.12856 * (distance()) + 3.75054)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0071083 * (1 - exp(-0.618786 * (distance() + 0.137655)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00803625 * (0.919661 - exp(-0.740644 * (enclose() + 2.96832) - -0.0191032 * (enclose() + 9.39334) * (enclose() + 9.39334)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0346512 * (1 - exp(-0.690512 * (distance() + 0.105724)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0368566
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0191345 * (distance()) + 0.56981) * (0.0978686 - exp(-(-0.103964 * (distance()) + 0.0588483) * (enclose() + 8.06237 * (distance()) + 16.4226) - (0.00035461 * (distance()) + 0.0242081) * (enclose() + 5.27684 * (distance()) + 6.97892) * (enclose() + 5.27684 * (distance()) + 6.97892)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0346512 * (1 - exp(-0.690512 * (distance() + 0.105724)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.332741 * (0.0978686 - exp(0.460972 * (enclose() + 56.7344) - 0.0259812 * (enclose() + 33.3631) * (enclose() + 33.3631)) + 0.0025934)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal3,metal4
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal1 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.93661 - 1.11783 * (distance())) + 0.00781258 / pow(distance() , 1.38524)) * 1.22166 * pow((width1() + width2()) / 2 , 0.0769799)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0304859 * (1 - exp(-0.777057 * (distance() + 0.0479711)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0313204
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00145576 * (distance()) + 0.117424) * (0.536373 - exp(-(0.0104351 * (distance()) + 1.08446) * (enclose() + 0.0859636 * (distance()) + 0.806821) - (0.0445781 * (distance()) + -0.327194) * (enclose() + -0.425306 * (distance()) + 1.09372) * (enclose() + -0.425306 * (distance()) + 1.09372)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0304859 * (1 - exp(-0.777057 * (distance() + 0.0479711)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0550726 * (0.536373 - exp(-1.13664 * (enclose() + 1.23664) - -0.104303 * (enclose() + -1.03281) * (enclose() + -1.03281)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0136969 * (1 - exp(-0.710729 * (distance() + 0.0584625)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0140225
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00104672 * (distance()) + 0.0410982) * (0.761635 - exp(-(0.259743 * (distance()) + 0.203863) * (enclose() + 0.0701919 * (distance()) + 1.01176) - (-0.0104975 * (distance()) + -0.0186284) * (enclose() + 0.524759 * (distance()) + 1.89003) * (enclose() + 0.524759 * (distance()) + 1.89003)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0136969 * (1 - exp(-0.710729 * (distance() + 0.0584625)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0179323 * (0.761635 - exp(-1.50258 * (enclose() + 1.36272) - -0.0711159 * (enclose() + 4.51382) * (enclose() + 4.51382)) + 0.00115063)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal3,metal5
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.99098 - 0.770886 * (distance())) + 0.0134595 / pow(distance() , 1.21893)) * 1.06873 * pow((width1() + width2()) / 2 , 0.104488)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0191152 * (1 - exp(-0.453173 * (distance() + 0.215724)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0191549
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00091856 * (distance()) + 0.0373032) * (0.842932 - exp(-(0.291293 * (distance()) + 0.294036) * (enclose() + 0.277432 * (distance()) + 0.905782) - (-0.00717012 * (distance()) + -0.0311357) * (enclose() + 0.916566 * (distance()) + 2.46705) * (enclose() + 0.916566 * (distance()) + 2.46705)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0191152 * (1 - exp(-0.453173 * (distance() + 0.215724)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.020948 * (0.842932 - exp(-1.7505 * (enclose() + 2.29294) - -0.0669863 * (enclose() + 7.04988) * (enclose() + 7.04988)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0192201 * (1 - exp(-0.450665 * (distance() + 0.213337)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0192311
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-2.03709e-05 * (distance()) + 0.0452433) * (0.821266 - exp(-(0.235758 * (distance()) + 0.271618) * (enclose() + 0.179401 * (distance()) + 1.26859) - (-0.00665557 * (distance()) + -0.0220781) * (enclose() + 0.700554 * (distance()) + 3.40064) * (enclose() + 0.700554 * (distance()) + 3.40064)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0192201 * (1 - exp(-0.450665 * (distance() + 0.213337)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0225707 * (0.821266 - exp(-1.45041 * (enclose() + 2.1656) - -0.055356 * (enclose() + 6.90341) * (enclose() + 6.90341)) + 0.00276648)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal3,metal6
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.4248 - 0.642257 * (distance())) + 0.0138054 / pow(distance() , 1.10332)) * 1.36381 * pow((width1() + width2()) / 2 , 0.116581)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0137538 * (1 - exp(-0.374282 * (distance() + 0.326261)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0139074
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00031951 * (distance()) + 0.026453) * (0.917997 - exp(-(0.163497 * (distance()) + 0.172776) * (enclose() + 0.475047 * (distance()) + 0.784665) - (-0.00247157 * (distance()) + -0.016272) * (enclose() + 1.33923 * (distance()) + 2.71552) * (enclose() + 1.33923 * (distance()) + 2.71552)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0137538 * (1 - exp(-0.374282 * (distance() + 0.326261)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0140253 * (0.917997 - exp(-0.990261 * (enclose() + 3.1599) - -0.0286298 * (enclose() + 9.41167) * (enclose() + 9.41167)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0221111 * (1 - exp(-0.383255 * (distance() + 0.310373)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0224279
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00109927 * (distance()) + 0.0463818) * (0.791046 - exp(-(0.214189 * (distance()) + 0.235759) * (enclose() + 0.164875 * (distance()) + 1.32168) - (-0.00664037 * (distance()) + -0.0155696) * (enclose() + 0.675185 * (distance()) + 3.44773) * (enclose() + 0.675185 * (distance()) + 3.44773)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0221111 * (1 - exp(-0.383255 * (distance() + 0.310373)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0259391 * (0.791046 - exp(-1.3067 * (enclose() + 2.14606) - -0.0487714 * (enclose() + 6.82366) * (enclose() + 6.82366)) + 0.00429166)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal4,metal5
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.99585 - 0.981688 * (distance())) + 0.0125161 / pow(distance() , 1.2435)) * 1.06554 * pow((width1() + width2()) / 2 , 0.0894411)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0330209 * (1 - exp(-0.70983 * (distance() + 0.0851382)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0345624
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00793221 * (distance()) + 0.120282) * (0.393489 - exp(-(-0.0769734 * (distance()) + 0.478863) * (enclose() + -0.938247 * (distance()) + 2.02869) - (0.0359558 * (distance()) + -0.00661762) * (enclose() + -0.49583 * (distance()) + 5.72529) * (enclose() + -0.49583 * (distance()) + 5.72529)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0330209 * (1 - exp(-0.70983 * (distance() + 0.0851382)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0799715 * (0.393489 - exp(-0.093996 * (enclose() + -2.66255) - 0.173161 * (enclose() + 3.24614) * (enclose() + 3.24614)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00955298 * (1 - exp(-0.636865 * (distance() + 0.10606)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00996306
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00072817 * (distance()) + 0.0267361) * (0.85096 - exp(-(0.164079 * (distance()) + 0.106259) * (enclose() + 0.1925 * (distance()) + 0.790093) - (-0.00476742 * (distance()) + -0.00731084) * (enclose() + 0.854144 * (distance()) + 2.00612) * (enclose() + 0.854144 * (distance()) + 2.00612)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00955298 * (1 - exp(-0.636865 * (distance() + 0.10606)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0115476 * (0.85096 - exp(-0.926654 * (enclose() + 1.75259) - -0.0311479 * (enclose() + 6.27684) * (enclose() + 6.27684)) + 0.00195953)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal4,metal6
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.51838 - 0.632915 * (distance())) + 0.0126789 / pow(distance() , 1.1011)) * 1.49368 * pow((width1() + width2()) / 2 , 0.117203)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0212416 * (1 - exp(-0.372859 * (distance() + 0.328573)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0214647
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00186103 * (distance()) + 0.035466) * (0.834347 - exp(-(0.2476 * (distance()) + 0.221125) * (enclose() + 0.350323 * (distance()) + 0.699683) - (-0.00569879 * (distance()) + -0.0241444) * (enclose() + 1.11659 * (distance()) + 1.67546) * (enclose() + 1.11659 * (distance()) + 1.67546)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0212416 * (1 - exp(-0.372859 * (distance() + 0.328573)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0223856 * (0.834347 - exp(-1.45913 * (enclose() + 2.4513) - -0.0526384 * (enclose() + 7.25841) * (enclose() + 7.25841)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0144999 * (1 - exp(-0.36595 * (distance() + 0.333613)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.014605
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00040033 * (distance()) + 0.0334442) * (0.895627 - exp(-(0.147605 * (distance()) + 0.156367) * (enclose() + 0.333809 * (distance()) + 1.0062) - (-0.00218288 * (distance()) + -0.0150079) * (enclose() + 1.20176 * (distance()) + 2.88844) * (enclose() + 1.20176 * (distance()) + 2.88844)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0144999 * (1 - exp(-0.36595 * (distance() + 0.333613)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0157213 * (0.895627 - exp(-0.894392 * (enclose() + 2.67525) - -0.0259223 * (enclose() + 8.89724) * (enclose() + 8.89724)) + 0.00438372)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.31079 - 0.876029 * (distance())) + 0.0132382 / pow(distance() , 1.14662)) * 1.26315 * pow((width1() + width2()) / 2 , 0.0999374)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0325003 * (1 - exp(-0.627407 * (distance() + 0.126217)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.034443
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00396693 * (distance()) + 0.0713094) * (0.660582 - exp(-(0.0963661 * (distance()) + 0.429111) * (enclose() + -0.162181 * (distance()) + 1.22495) - (0.00835555 * (distance()) + -0.00562963) * (enclose() + -0.436124 * (distance()) + 6.96836) * (enclose() + -0.436124 * (distance()) + 6.96836)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0325003 * (1 - exp(-0.627407 * (distance() + 0.126217)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.045572 * (0.660582 - exp(-0.910942 * (enclose() + 0.414045) - 0.0361481 * (enclose() + 4.78774) * (enclose() + 4.78774)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00777492 * (1 - exp(-0.568877 * (distance() + 0.154328)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00821127
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00089739 * (distance()) + 0.0228115) * (0.909961 - exp(-(0.113228 * (distance()) + 0.101888) * (enclose() + 0.315044 * (distance()) + 0.99211) - (-0.00188191 * (distance()) + -0.00698148) * (enclose() + 1.15945 * (distance()) + 3.59288) * (enclose() + 1.15945 * (distance()) + 3.59288)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00777492 * (1 - exp(-0.568877 * (distance() + 0.154328)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00916227 * (0.909961 - exp(-0.668028 * (enclose() + 2.56733) - -0.016391 * (enclose() + 9.39013) * (enclose() + 9.39013)) + 0.00283409)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal3,metal4
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal2 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.80613 - 1.54975 * (distance())) + 0.00348371 / pow(distance() , 1.66487)) * 1.39287 * pow((width1() + width2()) / 2 , 0.0452943)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal4 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0245125 * (1 - exp(-1.0753 * (distance() + -0.0261218)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0250101
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00039393 * (distance()) + 0.139155) * (0.345796 - exp(-(-0.0530249 * (distance()) + 0.051758) * (enclose() + 5.42489 * (distance()) + 2.55479) - (0.0183006 * (distance()) + 0.0613839) * (enclose() + 0.772213 * (distance()) + 3.40699) * (enclose() + 0.772213 * (distance()) + 3.40699)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0245125 * (1 - exp(-1.0753 * (distance() + -0.0261218)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0685927 * (0.345796 - exp(0.213366 * (enclose() + 29.6792) - 0.152887 * (enclose() + 7.26806) * (enclose() + 7.26806)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0247704 * (1 - exp(-1.06948 * (distance() + -0.0263717)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0252787
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00085389 * (distance()) + 0.208891) * (0.234647 - exp(-(-0.042572 * (distance()) + 0.0107256) * (enclose() + 15.6748 * (distance()) + 21.2554) - (0.00112014 * (distance()) + 0.0434278) * (enclose() + 3.28901 * (distance()) + 4.82355) * (enclose() + 3.28901 * (distance()) + 4.82355)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0247704 * (1 - exp(-1.06948 * (distance() + -0.0263717)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.102311 * (0.234647 - exp(0.202134 * (enclose() + 99.6294) - 0.0490285 * (enclose() + 21.2686) * (enclose() + 21.2686)) + 0.000348481)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal3,metal5
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal2 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.93467 - 1.11597 * (distance())) + 0.00774583 / pow(distance() , 1.39185)) * 1.21878 * pow((width1() + width2()) / 2 , 0.0773335)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0135969 * (1 - exp(-0.715094 * (distance() + 0.0641138)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0139182
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00021832 * (distance()) + 0.0351172) * (0.787367 - exp(-(0.334091 * (distance()) + 0.249449) * (enclose() + 0.178231 * (distance()) + 0.879141) - (-0.0125094 * (distance()) + -0.0240694) * (enclose() + 0.661 * (distance()) + 2.20767) * (enclose() + 0.661 * (distance()) + 2.20767)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0135969 * (1 - exp(-0.715094 * (distance() + 0.0641138)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0170128 * (0.787367 - exp(-1.9199 * (enclose() + 1.7703) - -0.0866164 * (enclose() + 5.51267) * (enclose() + 5.51267)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0307207 * (1 - exp(-0.778359 * (distance() + 0.0489958)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0315374
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00240761 * (distance()) + 0.163462) * (0.33264 - exp(-(-0.0989308 * (distance()) + 0.171919) * (enclose() + 2.16766 * (distance()) + 2.48267) - (0.0211415 * (distance()) + 0.0337599) * (enclose() + 0.544196 * (distance()) + 3.80926) * (enclose() + 0.544196 * (distance()) + 3.80926)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0307207 * (1 - exp(-0.778359 * (distance() + 0.0489958)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.08775 * (0.33264 - exp(0.322735 * (enclose() + 13.321) - 0.139467 * (enclose() + 6.53024) * (enclose() + 6.53024)) + 0.0011322)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal3,metal6
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.96858 - 0.976543 * (distance())) + 0.0132239 / pow(distance() , 1.23565)) * 1.02559 * pow((width1() + width2()) / 2 , 0.091565)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00921203 * (1 - exp(-0.638581 * (distance() + 0.11372)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00963461
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-2.43279e-05 * (distance()) + 0.0217153) * (0.850186 - exp(-(0.175127 * (distance()) + 0.0997806) * (enclose() + 0.318743 * (distance()) + 0.548157) - (-0.00542874 * (distance()) + -0.00364368) * (enclose() + 1.02783 * (distance()) + 1.56343) * (enclose() + 1.02783 * (distance()) + 1.56343)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00921203 * (1 - exp(-0.638581 * (distance() + 0.11372)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0107968 * (0.850186 - exp(-0.975416 * (enclose() + 2.14187) - -0.0307874 * (enclose() + 6.70258) * (enclose() + 6.70258)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0333938 * (1 - exp(-0.709276 * (distance() + 0.0874553)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0350046
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0105043 * (distance()) + 0.332237) * (0.164547 - exp(-(-0.0190288 * (distance()) + 0.0170498) * (enclose() + 13.3369 * (distance()) + 19.0779) - (0.00526743 * (distance()) + 0.0312518) * (enclose() + 1.21772 * (distance()) + 6.44282) * (enclose() + 1.21772 * (distance()) + 6.44282)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0333938 * (1 - exp(-0.709276 * (distance() + 0.0874553)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.192379 * (0.164547 - exp(0.0780942 * (enclose() + 85.7624) - 0.0575889 * (enclose() + 12.5314) * (enclose() + 12.5314)) + 0.00200736)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal4,metal5
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal2 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.93661 - 1.11783 * (distance())) + 0.00781258 / pow(distance() , 1.38524)) * 1.22166 * pow((width1() + width2()) / 2 , 0.0769799)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0304859 * (1 - exp(-0.777057 * (distance() + 0.0479711)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0313204
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0155504 * (distance()) + 0.859733) * (0.0619632 - exp(-(-0.169092 * (distance()) + 0.325353) * (enclose() + 1.60428 * (distance()) + 6.7004) - (0.0193418 * (distance()) + 0.00718074) * (enclose() + 0.538913 * (distance()) + 7.62898) * (enclose() + 0.538913 * (distance()) + 7.62898)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0304859 * (1 - exp(-0.777057 * (distance() + 0.0479711)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.468743 * (0.0619632 - exp(0.520107 * (enclose() + 14.7218) - 0.10389 * (enclose() + 10.3235) * (enclose() + 10.3235)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0136969 * (1 - exp(-0.710729 * (distance() + 0.0584625)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0140225
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00045353 * (distance()) + 0.0369977) * (0.778067 - exp(-(0.296724 * (distance()) + 0.201887) * (enclose() + 0.148656 * (distance()) + 0.880568) - (-0.0115353 * (distance()) + -0.0166042) * (enclose() + 0.660035 * (distance()) + 1.96579) * (enclose() + 0.660035 * (distance()) + 1.96579)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0136969 * (1 - exp(-0.710729 * (distance() + 0.0584625)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.017365 * (0.778067 - exp(-1.68551 * (enclose() + 1.62385) - -0.0742807 * (enclose() + 5.26597) * (enclose() + 5.26597)) + 0.00115063)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal4,metal6
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.08406 - 0.752598 * (distance())) + 0.012877 / pow(distance() , 1.20789)) * 1.15027 * pow((width1() + width2()) / 2 , 0.105881)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0184601 * (1 - exp(-0.443823 * (distance() + 0.223621)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0185149
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00107616 * (distance()) + 0.0368826) * (0.814314 - exp(-(0.260478 * (distance()) + 0.213959) * (enclose() + 0.325436 * (distance()) + 0.680931) - (-0.00628019 * (distance()) + -0.0275349) * (enclose() + 1.0626 * (distance()) + 1.41042) * (enclose() + 1.0626 * (distance()) + 1.41042)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0184601 * (1 - exp(-0.443823 * (distance() + 0.223621)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0211317 * (0.814314 - exp(-1.51635 * (enclose() + 2.30811) - -0.0589358 * (enclose() + 6.72342) * (enclose() + 6.72342)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0195349 * (1 - exp(-0.443891 * (distance() + 0.218639)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.01959
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00102449 * (distance()) + 0.0395348) * (0.819458 - exp(-(0.264628 * (distance()) + 0.254427) * (enclose() + 0.256241 * (distance()) + 1.14535) - (-0.0076653 * (distance()) + -0.0169196) * (enclose() + 0.825411 * (distance()) + 3.51248) * (enclose() + 0.825411 * (distance()) + 3.51248)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0195349 * (1 - exp(-0.443891 * (distance() + 0.218639)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0223286 * (0.819458 - exp(-1.57757 * (enclose() + 2.42656) - -0.0552461 * (enclose() + 7.63954) * (enclose() + 7.63954)) + 0.00292082)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.99503 - 0.927118 * (distance())) + 0.0131648 / pow(distance() , 1.2261)) * 1.0548 * pow((width1() + width2()) / 2 , 0.0927526)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0311468 * (1 - exp(-0.648377 * (distance() + 0.103285)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0324929
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0008901 * (distance()) + 0.071986) * (0.893575 - exp(-(-0.0478407 * (distance()) + 2.01809) * (enclose() + -0.151857 * (distance()) + 1.83284) - (0.0177448 * (distance()) + -0.191092) * (enclose() + -0.245935 * (distance()) + 4.41409) * (enclose() + -0.245935 * (distance()) + 4.41409)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0311468 * (1 - exp(-0.648377 * (distance() + 0.103285)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0337677 * (0.893575 - exp(-1.77889 * (enclose() + 1.07356) - -0.102368 * (enclose() + 3.18441) * (enclose() + 3.18441)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0101154 * (1 - exp(-0.590889 * (distance() + 0.125475)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0105052
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00036055 * (distance()) + 0.0252421) * (0.862327 - exp(-(0.180328 * (distance()) + 0.122643) * (enclose() + 0.2904 * (distance()) + 0.714953) - (-0.0043461 * (distance()) + -0.00964572) * (enclose() + 1.0581 * (distance()) + 2.15815) * (enclose() + 1.0581 * (distance()) + 2.15815)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0101154 * (1 - exp(-0.590889 * (distance() + 0.125475)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0117197 * (0.862327 - exp(-1.02428 * (enclose() + 2.16695) - -0.0313762 * (enclose() + 7.44865) * (enclose() + 7.44865)) + 0.00217008)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal4,metal5
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal3 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.80613 - 1.54975 * (distance())) + 0.00348371 / pow(distance() , 1.66487)) * 1.39287 * pow((width1() + width2()) / 2 , 0.0452943)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0245125 * (1 - exp(-1.0753 * (distance() + -0.0261218)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0250101
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00144842 * (distance()) + 0.322258) * (0.150659 - exp(-(-0.00733732 * (distance()) + -0.00463333) * (enclose() + 47.5595 * (distance()) + 26.475) - (0.00990981 * (distance()) + 0.0300525) * (enclose() + 1.05445 * (distance()) + 7.74391) * (enclose() + 1.05445 * (distance()) + 7.74391)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0245125 * (1 - exp(-1.0753 * (distance() + -0.0261218)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.157508 * (0.150659 - exp(0.0413199 * (enclose() + 264.273) - 0.0796015 * (enclose() + 13.0162) * (enclose() + 13.0162)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0247704 * (1 - exp(-1.06948 * (distance() + -0.0263717)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0252787
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0007043 * (distance()) + 0.148824) * (0.330055 - exp(-(-0.040783 * (distance()) + 0.0330215) * (enclose() + 7.36549 * (distance()) + 3.53407) - (0.0132388 * (distance()) + 0.059025) * (enclose() + 0.936208 * (distance()) + 3.62808) * (enclose() + 0.936208 * (distance()) + 3.62808)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0247704 * (1 - exp(-1.06948 * (distance() + -0.0263717)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0726513 * (0.330055 - exp(0.170894 * (enclose() + 40.3615) - 0.125219 * (enclose() + 8.30912) * (enclose() + 8.30912)) + 0.000348481)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal4,metal6
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.93958 - 1.10061 * (distance())) + 0.00820921 / pow(distance() , 1.36818)) * 1.20404 * pow((width1() + width2()) / 2 , 0.0797617)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0130433 * (1 - exp(-0.703641 * (distance() + 0.070565)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0133793
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00023248 * (distance()) + 0.0342283) * (0.732565 - exp(-(0.179599 * (distance()) + 0.156221) * (enclose() + 0.089547 * (distance()) + 1.05381) - (-4.21978e-05 * (distance()) + 8.41531e-05) * (enclose() + -14.2549 * (distance()) + 5.03428) * (enclose() + -14.2549 * (distance()) + 5.03428)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0130433 * (1 - exp(-0.703641 * (distance() + 0.070565)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.016533 * (0.732565 - exp(-1.05422 * (enclose() + 1.50154) - -0.000126836 * (enclose() + -66.2402) * (enclose() + -66.2402)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0310526 * (1 - exp(-0.7677 * (distance() + 0.0546078)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0319422
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00440981 * (distance()) + 0.256564) * (0.211664 - exp(-(-0.116323 * (distance()) + 0.209667) * (enclose() + 1.92347 * (distance()) + 3.94648) - (0.0206217 * (distance()) + 0.0220593) * (enclose() + 0.521509 * (distance()) + 4.96436) * (enclose() + 0.521509 * (distance()) + 4.96436)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0310526 * (1 - exp(-0.7677 * (distance() + 0.0546078)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.139307 * (0.211664 - exp(0.371948 * (enclose() + 13.5638) - 0.125168 * (enclose() + 7.57191) * (enclose() + 7.57191)) + 0.00122188)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.93033 - 1.0601 * (distance())) + 0.00836617 / pow(distance() , 1.3652)) * 1.19694 * pow((width1() + width2()) / 2 , 0.0802277)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0285752 * (1 - exp(-0.714555 * (distance() + 0.0624121)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0292614
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00113394 * (distance()) + 0.06963) * (0.863896 - exp(-(-0.0357247 * (distance()) + 2.06099) * (enclose() + -0.14415 * (distance()) + 1.66714) - (0.0216202 * (distance()) + -0.213361) * (enclose() + -0.236018 * (distance()) + 4.01447) * (enclose() + -0.236018 * (distance()) + 4.01447)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0285752 * (1 - exp(-0.714555 * (distance() + 0.0624121)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0319801 * (0.863896 - exp(-1.88237 * (enclose() + 0.94639) - -0.10526 * (enclose() + 2.83438) * (enclose() + 2.83438)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0144104 * (1 - exp(-0.663653 * (distance() + 0.0745186)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0146944
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00067065 * (distance()) + 0.0388253) * (0.749185 - exp(-(0.13208 * (distance()) + 0.153423) * (enclose() + 0.533652 * (distance()) + 0.996137) - (-0.00038959 * (distance()) + -0.00384914) * (enclose() + -3.83565 * (distance()) + -0.270006) * (enclose() + -3.83565 * (distance()) + -0.270006)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0144104 * (1 - exp(-0.663653 * (distance() + 0.0745186)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.017736 * (0.749185 - exp(-0.813823 * (enclose() + 3.6644) - -0.00579709 * (enclose() + -19.4483) * (enclose() + -19.4483)) + 0.00129246)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal4,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal4 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.83034 - 1.48697 * (distance())) + 0.00384506 / pow(distance() , 1.61138)) * 1.39811 * pow((width1() + width2()) / 2 , 0.0495312)
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal4,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0226103 * (1 - exp(-0.999149 * (distance() + -0.0146251)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0230884
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00024909 * (distance()) + 0.0750589) * (0.593892 - exp(-(-0.0170035 * (distance()) + 1.46695) * (enclose() + 0.0602361 * (distance()) + 0.725667) - (0.10629 * (distance()) + -0.360058) * (enclose() + -0.266446 * (distance()) + 1.44709) * (enclose() + -0.266446 * (distance()) + 1.44709)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0226103 * (1 - exp(-0.999149 * (distance() + -0.0146251)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0369067 * (0.593892 - exp(-1.38193 * (enclose() + 1.02685) - 0.171392 * (enclose() + 0.11486) * (enclose() + 0.11486)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal4,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.025663 * (1 - exp(-1.01037 * (distance() + -0.0152777)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0262083
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00043106 * (distance()) + 0.19162) * (0.261616 - exp(-(-0.0259876 * (distance()) + 0.0150874) * (enclose() + 11.0929 * (distance()) + 7.25678) - (0.0126574 * (distance()) + 0.0455591) * (enclose() + 0.888697 * (distance()) + 4.74275) * (enclose() + 0.888697 * (distance()) + 4.74275)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.025663 * (1 - exp(-1.01037 * (distance() + -0.0152777)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0947324 * (0.261616 - exp(0.114851 * (enclose() + 62.7213) - 0.108846 * (enclose() + 9.18623) * (enclose() + 9.18623)) + 0.000423073)
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE NEARBODY poly metal1 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = length() * (0.00678279 * exp(-3.97673 * (distance() - -0.01)) + 0.00683101 * exp(-6.97517 * (distance() - -0.01)))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE NEARBODY poly metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = length() * (0.00484581 * exp(-1.32021 * (distance() - -0.01)) + 0.00484518 * exp(-1.32406 * (distance() - -0.01)))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE NEARBODY poly metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = length() * (0.00435037 * exp(-0.774519 * (distance() - -0.01)) + 0.00435039 * exp(-0.787307 * (distance() - -0.01)))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE NEARBODY poly metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = length() * (0.00414412 * exp(-0.542912 * (distance() - -0.01)) + 0.00414411 * exp(-0.543535 * (distance() - -0.01)))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE NEARBODY poly metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = length() * (0.00403024 * exp(-0.416175 * (distance() - -0.01)) + 0.00403024 * exp(-0.418467 * (distance() - -0.01)))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE NEARBODY poly metal6 MASK
[
    PROPERTY C
    max_caldistance = 15 // For reference
    max_distance = 3
    C = length() * (0.0046635 * exp(-0.319814 * (distance() - -0.01)) + 0.00466352 * exp(-0.319209 * (distance() - -0.01)))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE NEARBODY metal1 metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = length() * (0.00542267 * exp(-2.87466 * (distance())) + 0.00541197 * exp(-2.85594 * (distance())))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE NEARBODY metal1 metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = length() * (0.00462335 * exp(-1.12191 * (distance())) + 0.0046231 * exp(-1.11743 * (distance())))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE NEARBODY metal1 metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = length() * (0.00430419 * exp(-0.700016 * (distance())) + 0.00430419 * exp(-0.691001 * (distance())))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE NEARBODY metal1 metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = length() * (0.00412103 * exp(-0.504454 * (distance())) + 0.00412103 * exp(-0.504715 * (distance())))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE NEARBODY metal1 metal6 MASK
[
    PROPERTY C
    max_caldistance = 15 // For reference
    max_distance = 3
    C = length() * (0.00492359 * exp(-0.366735 * (distance())) + 0.00492357 * exp(-0.366271 * (distance())))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE NEARBODY metal2 metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = length() * (0.00529583 * exp(-2.92457 * (distance())) + 0.00528042 * exp(-2.92589 * (distance())))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE NEARBODY metal2 metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = length() * (0.00440038 * exp(-1.10781 * (distance())) + 0.00440019 * exp(-1.10914 * (distance())))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE NEARBODY metal2 metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = length() * (0.00430139 * exp(-0.698033 * (distance())) + 0.00430138 * exp(-0.69715 * (distance())))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE NEARBODY metal2 metal6 MASK
[
    PROPERTY C
    max_caldistance = 15 // For reference
    max_distance = 3
    C = length() * (0.00526166 * exp(-0.460485 * (distance())) + 0.00526166 * exp(-0.459834 * (distance())))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE NEARBODY metal3 metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = length() * (0.00528228 * exp(-2.95429 * (distance())) + 0.00528382 * exp(-2.91037 * (distance())))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE NEARBODY metal3 metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = length() * (0.00431841 * exp(-1.14567 * (distance())) + 0.00431908 * exp(-1.13754 * (distance())))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE NEARBODY metal3 metal6 MASK
[
    PROPERTY C
    max_caldistance = 15 // For reference
    max_distance = 3
    C = length() * (0.00580325 * exp(-0.625501 * (distance())) + 0.00580315 * exp(-0.625834 * (distance())))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE NEARBODY metal4 metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = length() * (0.00526121 * exp(-2.92627 * (distance())) + 0.00530726 * exp(-2.9391 * (distance())))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE NEARBODY metal4 metal6 MASK
[
    PROPERTY C
    max_caldistance = 15 // For reference
    max_distance = 3
    C = length() * (0.00706099 * exp(-0.936613 * (distance())) + 0.00706082 * exp(-0.93763 * (distance())))
]
//
//  model: 14  target: nearbody2  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE NEARBODY metal5 metal6 MASK
[
    PROPERTY C
    max_caldistance = 15 // For reference
    max_distance = 3
    C = length() * (0.0125038 * exp(-1.66381 * (distance())) + 0.0125051 * exp(-1.66059 * (distance())))
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal1,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal1 tndiff MASK
[
    PROPERTY C
    C = 0.0505943 * area()
]
CAPACITANCE CROSSOVER PLATE metal1 tpdiff MASK
[
    PROPERTY C
    C = 0.0505943 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal2,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal2 tndiff MASK
[
    PROPERTY C
    C = 0.0163514 * area()
]
CAPACITANCE CROSSOVER PLATE metal2 tpdiff MASK
[
    PROPERTY C
    C = 0.0163514 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal3 tndiff MASK
[
    PROPERTY C
    C = 0.00975246 * area()
]
CAPACITANCE CROSSOVER PLATE metal3 tpdiff MASK
[
    PROPERTY C
    C = 0.00975246 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal4 tndiff MASK
[
    PROPERTY C
    C = 0.00694865 * area()
]
CAPACITANCE CROSSOVER PLATE metal4 tpdiff MASK
[
    PROPERTY C
    C = 0.00694865 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal5 tndiff MASK
[
    PROPERTY C
    C = 0.00539729 * area()
]
CAPACITANCE CROSSOVER PLATE metal5 tpdiff MASK
[
    PROPERTY C
    C = 0.00539729 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal6 tndiff MASK
[
    PROPERTY C
    C = 0.00433502 * area()
]
CAPACITANCE CROSSOVER PLATE metal6 tpdiff MASK
[
    PROPERTY C
    C = 0.00433502 * area()
]
//
//  model: 34  target: intrinsic  profile: active  layers: active_base,metal1,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0423217 * (1 - exp(-0.796514 * (distance() + 0.111811)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0455991
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0423217 * (1 - exp(-0.796514 * (distance() + 0.111811)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0455991
    }
]
//
//  model: 34  target: nearbody  profile: active  layers: active_base,metal1,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.82476 - 1.08441 * (distance())) + 0.0133754 / pow(distance() , 1.02707)) * 1.5936 * pow((width1() + width2()) / 2 , 0.0977082)
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.82476 - 1.08441 * (distance())) + 0.0133754 / pow(distance() , 1.02707)) * 1.5936 * pow((width1() + width2()) / 2 , 0.0977082)
]
//
//  model: 34  target: intrinsic  profile: active  layers: active_base,metal2,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0300837 * (1 - exp(-0.391872 * (distance() + 0.421648)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0336332
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0300837 * (1 - exp(-0.391872 * (distance() + 0.421648)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0336332
    }
]
//
//  model: 34  target: nearbody  profile: active  layers: active_base,metal2,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.13632 - 0.520214 * (distance())) + 0.0223166 / pow(distance() , 0.880542)) * 1.4129 * pow((width1() + width2()) / 2 , 0.130266)
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.13632 - 0.520214 * (distance())) + 0.0223166 / pow(distance() , 0.880542)) * 1.4129 * pow((width1() + width2()) / 2 , 0.130266)
]
//
//  model: 34  target: intrinsic  profile: active  layers: active_base,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0238011 * (1 - exp(-0.323203 * (distance() + 0.67418)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0292193
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0238011 * (1 - exp(-0.323203 * (distance() + 0.67418)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0292193
    }
]
//
//  model: 34  target: nearbody  profile: active  layers: active_base,metal3,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.64424 - 0.312993 * (distance())) + 0.020412 / pow(distance() , 0.826111)) * 1.77689 * pow((width1() + width2()) / 2 , 0.142956)
]
CAPACITANCE NEARBODY metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.64424 - 0.312993 * (distance())) + 0.020412 / pow(distance() , 0.826111)) * 1.77689 * pow((width1() + width2()) / 2 , 0.142956)
]
//
//  model: 34  target: intrinsic  profile: active  layers: active_base,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0191673 * (1 - exp(-0.324211 * (distance() + 0.8133)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0265146
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0191673 * (1 - exp(-0.324211 * (distance() + 0.8133)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0265146
    }
]
//
//  model: 34  target: nearbody  profile: active  layers: active_base,metal4,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.6872 - 0.205191 * (distance())) + 0.0220876 / pow(distance() , 0.809799)) * 1.7137 * pow((width1() + width2()) / 2 , 0.148275)
]
CAPACITANCE NEARBODY metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.6872 - 0.205191 * (distance())) + 0.0220876 / pow(distance() , 0.809799)) * 1.7137 * pow((width1() + width2()) / 2 , 0.148275)
]
//
//  model: 34  target: intrinsic  profile: active  layers: active_base,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal5 tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0157623 * (1 - exp(-0.347418 * (distance() + 0.890055)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0245306
    }
]
CAPACITANCE CROSSOVER FRINGE metal5 tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0157623 * (1 - exp(-0.347418 * (distance() + 0.890055)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0245306
    }
]
//
//  model: 34  target: nearbody  profile: active  layers: active_base,metal5,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.58905 - 0.146503 * (distance())) + 0.0234876 / pow(distance() , 0.809508)) * 1.61878 * pow((width1() + width2()) / 2 , 0.152804)
]
CAPACITANCE NEARBODY metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-4.58905 - 0.146503 * (distance())) + 0.0234876 / pow(distance() , 0.809508)) * 1.61878 * pow((width1() + width2()) / 2 , 0.152804)
]
//
//  model: 34  target: intrinsic  profile: active  layers: active_base,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal6 tndiff MASK
[
    PROPERTY C
    max_caldistance = 15 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0383273 * (1 - exp(-0.0554744 * (distance() + 0.976134)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0290662
    }
]
CAPACITANCE CROSSOVER FRINGE metal6 tpdiff MASK
[
    PROPERTY C
    max_caldistance = 15 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0383273 * (1 - exp(-0.0554744 * (distance() + 0.976134)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0290662
    }
]
//
//  model: 34  target: nearbody  profile: active  layers: active_base,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 15 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.73277 - 0.242929 * (distance())) + 0.02316 / pow(distance() , 58.3533)) * 1.85853 * pow((width1() + width2()) / 2 , 0.0615039)
]
CAPACITANCE NEARBODY metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 15 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.73277 - 0.242929 * (distance())) + 0.02316 / pow(distance() , 58.3533)) * 1.85853 * pow((width1() + width2()) / 2 , 0.0615039)
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal2,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.03215 - 1.7385 * (distance())) + 0.00259593 / pow(distance() , 1.60638)) * 1.84633 * pow((width1() + width2()) / 2 , 0.0476)
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.03215 - 1.7385 * (distance())) + 0.00259593 / pow(distance() , 1.60638)) * 1.84633 * pow((width1() + width2()) / 2 , 0.0476)
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal2,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal2 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0229737 * (1 - exp(-1.15836 * (distance() + -0.0156682)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0235791
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00087237 * (distance()) + 0.113148) * (0.409415 - exp(-(-0.0216615 * (distance()) + 0.00467498) * (enclose() + 13.6765 * (distance()) + -3.14365) - (0.0141517 * (distance()) + 0.0723357) * (enclose() + 0.903522 * (distance()) + 3.12541) * (enclose() + 0.903522 * (distance()) + 3.12541)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0229737 * (1 - exp(-1.15836 * (distance() + -0.0156682)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0543931 * (0.409415 - exp(0.103633 * (enclose() + 65.2389) - 0.143094 * (enclose() + 7.64302) * (enclose() + 7.64302)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal2 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0229737 * (1 - exp(-1.15836 * (distance() + -0.0156682)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0235791
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00087237 * (distance()) + 0.113148) * (0.409415 - exp(-(-0.0216615 * (distance()) + 0.00467498) * (enclose() + 13.6765 * (distance()) + -3.14365) - (0.0141517 * (distance()) + 0.0723357) * (enclose() + 0.903522 * (distance()) + 3.12541) * (enclose() + 0.903522 * (distance()) + 3.12541)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0229737 * (1 - exp(-1.15836 * (distance() + -0.0156682)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0543931 * (0.409415 - exp(0.103633 * (enclose() + 65.2389) - 0.143094 * (enclose() + 7.64302) * (enclose() + 7.64302)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal2,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0278489 * (1 - exp(-1.17925 * (distance() + -0.0119009)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0286158
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal2 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0278489 * (1 - exp(-1.17925 * (distance() + -0.0119009)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0286158
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal3,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.09845 - 1.27179 * (distance())) + 0.00632588 / pow(distance() , 1.35545)) * 1.47898 * pow((width1() + width2()) / 2 , 0.0744597)
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.09845 - 1.27179 * (distance())) + 0.00632588 / pow(distance() , 1.35545)) * 1.47898 * pow((width1() + width2()) / 2 , 0.0744597)
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0124068 * (1 - exp(-0.801153 * (distance() + 0.0594665)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0128154
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00038857 * (distance()) + 0.0323395) * (0.803566 - exp(-(0.346872 * (distance()) + 0.224035) * (enclose() + 0.168399 * (distance()) + 0.724396) - (-0.0133736 * (distance()) + -0.0255915) * (enclose() + 0.68146 * (distance()) + 1.59596) * (enclose() + 0.68146 * (distance()) + 1.59596)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0124068 * (1 - exp(-0.801153 * (distance() + 0.0594665)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0151983 * (0.803566 - exp(-1.9584 * (enclose() + 1.56639) - -0.0924595 * (enclose() + 5.00326) * (enclose() + 5.00326)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0124068 * (1 - exp(-0.801153 * (distance() + 0.0594665)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0128154
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00038857 * (distance()) + 0.0323395) * (0.803566 - exp(-(0.346872 * (distance()) + 0.224035) * (enclose() + 0.168399 * (distance()) + 0.724396) - (-0.0133736 * (distance()) + -0.0255915) * (enclose() + 0.68146 * (distance()) + 1.59596) * (enclose() + 0.68146 * (distance()) + 1.59596)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0124068 * (1 - exp(-0.801153 * (distance() + 0.0594665)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0151983 * (0.803566 - exp(-1.9584 * (enclose() + 1.56639) - -0.0924595 * (enclose() + 5.00326) * (enclose() + 5.00326)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0337266 * (1 - exp(-0.891953 * (distance() + 0.0464785)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0348976
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0337266 * (1 - exp(-0.891953 * (distance() + 0.0464785)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0348976
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal4,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.31669 - 1.13618 * (distance())) + 0.00844321 / pow(distance() , 1.23332)) * 1.5169 * pow((width1() + width2()) / 2 , 0.0844325)
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.31669 - 1.13618 * (distance())) + 0.00844321 / pow(distance() , 1.23332)) * 1.5169 * pow((width1() + width2()) / 2 , 0.0844325)
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00856675 * (1 - exp(-0.728993 * (distance() + 0.0965313)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00900764
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00037148 * (distance()) + 0.0220125) * (0.853034 - exp(-(0.207732 * (distance()) + 0.150067) * (enclose() + 0.240597 * (distance()) + 0.718432) - (-0.00554149 * (distance()) + -0.0147603) * (enclose() + 0.899593 * (distance()) + 1.79468) * (enclose() + 0.899593 * (distance()) + 1.79468)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00856675 * (1 - exp(-0.728993 * (distance() + 0.0965313)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0100776 * (0.853034 - exp(-1.18873 * (enclose() + 1.92142) - -0.0424677 * (enclose() + 6.29265) * (enclose() + 6.29265)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00856675 * (1 - exp(-0.728993 * (distance() + 0.0965313)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00900764
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00037148 * (distance()) + 0.0220125) * (0.853034 - exp(-(0.207732 * (distance()) + 0.150067) * (enclose() + 0.240597 * (distance()) + 0.718432) - (-0.00554149 * (distance()) + -0.0147603) * (enclose() + 0.899593 * (distance()) + 1.79468) * (enclose() + 0.899593 * (distance()) + 1.79468)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00856675 * (1 - exp(-0.728993 * (distance() + 0.0965313)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0100776 * (0.853034 - exp(-1.18873 * (enclose() + 1.92142) - -0.0424677 * (enclose() + 6.29265) * (enclose() + 6.29265)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0361489 * (1 - exp(-0.826112 * (distance() + 0.074354)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0380392
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0361489 * (1 - exp(-0.826112 * (distance() + 0.074354)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0380392
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal5,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.36617 - 1.08875 * (distance())) + 0.0108818 / pow(distance() , 1.16448)) * 1.39976 * pow((width1() + width2()) / 2 , 0.0900912)
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.36617 - 1.08875 * (distance())) + 0.0108818 / pow(distance() , 1.16448)) * 1.39976 * pow((width1() + width2()) / 2 , 0.0900912)
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00653072 * (1 - exp(-0.709227 * (distance() + 0.114458)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00698139
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00064309 * (distance()) + 0.0192192) * (0.863242 - exp(-(0.132185 * (distance()) + 0.138522) * (enclose() + 0.234222 * (distance()) + 0.913084) - (-0.00250601 * (distance()) + -0.0144334) * (enclose() + 0.90447 * (distance()) + 2.04258) * (enclose() + 0.90447 * (distance()) + 2.04258)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00653072 * (1 - exp(-0.709227 * (distance() + 0.114458)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00800187 * (0.863242 - exp(-0.799447 * (enclose() + 2.08419) - -0.0269634 * (enclose() + 6.56493) * (enclose() + 6.56493)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00653072 * (1 - exp(-0.709227 * (distance() + 0.114458)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00698139
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00064309 * (distance()) + 0.0192192) * (0.863242 - exp(-(0.132185 * (distance()) + 0.138522) * (enclose() + 0.234222 * (distance()) + 0.913084) - (-0.00250601 * (distance()) + -0.0144334) * (enclose() + 0.90447 * (distance()) + 2.04258) * (enclose() + 0.90447 * (distance()) + 2.04258)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00653072 * (1 - exp(-0.709227 * (distance() + 0.114458)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00800187 * (0.863242 - exp(-0.799447 * (enclose() + 2.08419) - -0.0269634 * (enclose() + 6.56493) * (enclose() + 6.56493)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.037396 * (1 - exp(-0.807547 * (distance() + 0.087198)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0399388
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.037396 * (1 - exp(-0.807547 * (distance() + 0.087198)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0399388
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.44895 - 1.07361 * (distance())) + 0.012081 / pow(distance() , 1.12314)) * 1.39491 * pow((width1() + width2()) / 2 , 0.0923899)
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.44895 - 1.07361 * (distance())) + 0.012081 / pow(distance() , 1.12314)) * 1.39491 * pow((width1() + width2()) / 2 , 0.0923899)
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00518184 * (1 - exp(-0.704302 * (distance() + 0.12288)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0056033
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00051841 * (distance()) + 0.0153279) * (0.892254 - exp(-(0.0829765 * (distance()) + 0.0826528) * (enclose() + 0.327886 * (distance()) + 0.89668) - (-0.00174136 * (distance()) + -0.00376229) * (enclose() + 0.981168 * (distance()) + 3.12441) * (enclose() + 0.981168 * (distance()) + 3.12441)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00518184 * (1 - exp(-0.704302 * (distance() + 0.12288)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00636792 * (0.892254 - exp(-0.497535 * (enclose() + 2.53611) - -0.0124691 * (enclose() + 8.03025) * (enclose() + 8.03025)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.00518184 * (1 - exp(-0.704302 * (distance() + 0.12288)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0056033
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00051841 * (distance()) + 0.0153279) * (0.892254 - exp(-(0.0829765 * (distance()) + 0.0826528) * (enclose() + 0.327886 * (distance()) + 0.89668) - (-0.00174136 * (distance()) + -0.00376229) * (enclose() + 0.981168 * (distance()) + 3.12441) * (enclose() + 0.981168 * (distance()) + 3.12441)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.00518184 * (1 - exp(-0.704302 * (distance() + 0.12288)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00636792 * (0.892254 - exp(-0.497535 * (enclose() + 2.53611) - -0.0124691 * (enclose() + 8.03025) * (enclose() + 8.03025)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0382529 * (1 - exp(-0.801695 * (distance() + 0.0933676)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0412576
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0382529 * (1 - exp(-0.801695 * (distance() + 0.0933676)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0412576
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal2,metal3,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.96851 - 1.14864 * (distance())) + 0.00685472 / pow(distance() , 1.41225)) * 1.30284 * pow((width1() + width2()) / 2 , 0.0741397)
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.96851 - 1.14864 * (distance())) + 0.00685472 / pow(distance() , 1.41225)) * 1.30284 * pow((width1() + width2()) / 2 , 0.0741397)
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal2,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0299489 * (1 - exp(-0.798879 * (distance() + 0.0387284)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0307787
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0152183 * (distance()) + 1.04526) * (0.0509923 - exp(-(-0.195636 * (distance()) + 0.410011) * (enclose() + 1.39842 * (distance()) + 6.55607) - (0.0212116 * (distance()) + 0.00174168) * (enclose() + 0.501846 * (distance()) + 7.73403) * (enclose() + 0.501846 * (distance()) + 7.73403)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0299489 * (1 - exp(-0.798879 * (distance() + 0.0387284)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.560676 * (0.0509923 - exp(0.568169 * (enclose() + 13.5482) - 0.1078 * (enclose() + 10.2433) * (enclose() + 10.2433)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0299489 * (1 - exp(-0.798879 * (distance() + 0.0387284)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0307787
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0152183 * (distance()) + 1.04526) * (0.0509923 - exp(-(-0.195636 * (distance()) + 0.410011) * (enclose() + 1.39842 * (distance()) + 6.55607) - (0.0212116 * (distance()) + 0.00174168) * (enclose() + 0.501846 * (distance()) + 7.73403) * (enclose() + 0.501846 * (distance()) + 7.73403)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0299489 * (1 - exp(-0.798879 * (distance() + 0.0387284)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.560676 * (0.0509923 - exp(0.568169 * (enclose() + 13.5482) - 0.1078 * (enclose() + 10.2433) * (enclose() + 10.2433)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal2,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0145075 * (1 - exp(-0.733249 * (distance() + 0.0487125)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0148635
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0145075 * (1 - exp(-0.733249 * (distance() + 0.0487125)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0148635
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal2,metal4,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.98186 - 0.796462 * (distance())) + 0.0126454 / pow(distance() , 1.23756)) * 1.08845 * pow((width1() + width2()) / 2 , 0.102157)
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.98186 - 0.796462 * (distance())) + 0.0126454 / pow(distance() , 1.23756)) * 1.08845 * pow((width1() + width2()) / 2 , 0.102157)
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal2,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0185922 * (1 - exp(-0.469642 * (distance() + 0.198959)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0186587
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00103428 * (distance()) + 0.0375479) * (0.825032 - exp(-(0.293298 * (distance()) + 0.297585) * (enclose() + 0.262029 * (distance()) + 1.05631) - (-0.00873575 * (distance()) + -0.024678) * (enclose() + 0.804323 * (distance()) + 3.11107) * (enclose() + 0.804323 * (distance()) + 3.11107)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0185922 * (1 - exp(-0.469642 * (distance() + 0.198959)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0213597 * (0.825032 - exp(-1.76408 * (enclose() + 2.36646) - -0.0683567 * (enclose() + 7.13268) * (enclose() + 7.13268)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0185922 * (1 - exp(-0.469642 * (distance() + 0.198959)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0186587
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00103428 * (distance()) + 0.0375479) * (0.825032 - exp(-(0.293298 * (distance()) + 0.297585) * (enclose() + 0.262029 * (distance()) + 1.05631) - (-0.00873575 * (distance()) + -0.024678) * (enclose() + 0.804323 * (distance()) + 3.11107) * (enclose() + 0.804323 * (distance()) + 3.11107)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0185922 * (1 - exp(-0.469642 * (distance() + 0.198959)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0213597 * (0.825032 - exp(-1.76408 * (enclose() + 2.36646) - -0.0683567 * (enclose() + 7.13268) * (enclose() + 7.13268)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal2,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0201156 * (1 - exp(-0.471566 * (distance() + 0.192486)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0202096
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0201156 * (1 - exp(-0.471566 * (distance() + 0.192486)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0202096
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal2,metal5,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.28683 - 0.67493 * (distance())) + 0.0144752 / pow(distance() , 1.12444)) * 1.23615 * pow((width1() + width2()) / 2 , 0.113231)
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.28683 - 0.67493 * (distance())) + 0.0144752 / pow(distance() , 1.12444)) * 1.23615 * pow((width1() + width2()) / 2 , 0.113231)
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal2,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0136488 * (1 - exp(-0.393975 * (distance() + 0.29393)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0137883
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-8.04964e-05 * (distance()) + 0.0304993) * (0.880581 - exp(-(0.182192 * (distance()) + 0.2112) * (enclose() + 0.407266 * (distance()) + 0.91242) - (-0.00218136 * (distance()) + -0.0251135) * (enclose() + 1.2755 * (distance()) + 2.39039) * (enclose() + 1.2755 * (distance()) + 2.39039)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0136488 * (1 - exp(-0.393975 * (distance() + 0.29393)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0150484 * (0.880581 - exp(-1.12216 * (enclose() + 2.94875) - -0.0360203 * (enclose() + 8.76789) * (enclose() + 8.76789)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0136488 * (1 - exp(-0.393975 * (distance() + 0.29393)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0137883
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-8.04964e-05 * (distance()) + 0.0304993) * (0.880581 - exp(-(0.182192 * (distance()) + 0.2112) * (enclose() + 0.407266 * (distance()) + 0.91242) - (-0.00218136 * (distance()) + -0.0251135) * (enclose() + 1.2755 * (distance()) + 2.39039) * (enclose() + 1.2755 * (distance()) + 2.39039)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0136488 * (1 - exp(-0.393975 * (distance() + 0.29393)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0150484 * (0.880581 - exp(-1.12216 * (enclose() + 2.94875) - -0.0360203 * (enclose() + 8.76789) * (enclose() + 8.76789)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal2,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0228678 * (1 - exp(-0.405031 * (distance() + 0.277242)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0231833
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0228678 * (1 - exp(-0.405031 * (distance() + 0.277242)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0231833
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal2,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.38383 - 0.615098 * (distance())) + 0.0180972 / pow(distance() , 1.05031)) * 1.17282 * pow((width1() + width2()) / 2 , 0.119701)
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.38383 - 0.615098 * (distance())) + 0.0180972 / pow(distance() , 1.05031)) * 1.17282 * pow((width1() + width2()) / 2 , 0.119701)
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal2,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0104534 * (1 - exp(-0.373676 * (distance() + 0.349323)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0108505
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00049849 * (distance()) + 0.0247815) * (0.957796 - exp(-(0.1053 * (distance()) + 0.158086) * (enclose() + 0.537992 * (distance()) + 1.22189) - (-0.00093547 * (distance()) + -0.0115038) * (enclose() + 1.49373 * (distance()) + 4.30736) * (enclose() + 1.49373 * (distance()) + 4.30736)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0104534 * (1 - exp(-0.373676 * (distance() + 0.349323)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0111445 * (0.957796 - exp(-0.684586 * (enclose() + 3.91185) - -0.0161811 * (enclose() + 11.776) * (enclose() + 11.776)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0104534 * (1 - exp(-0.373676 * (distance() + 0.349323)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0108505
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00049849 * (distance()) + 0.0247815) * (0.957796 - exp(-(0.1053 * (distance()) + 0.158086) * (enclose() + 0.537992 * (distance()) + 1.22189) - (-0.00093547 * (distance()) + -0.0115038) * (enclose() + 1.49373 * (distance()) + 4.30736) * (enclose() + 1.49373 * (distance()) + 4.30736)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0104534 * (1 - exp(-0.373676 * (distance() + 0.349323)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0111445 * (0.957796 - exp(-0.684586 * (enclose() + 3.91185) - -0.0161811 * (enclose() + 11.776) * (enclose() + 11.776)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal2,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0243516 * (1 - exp(-0.387181 * (distance() + 0.324893)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0253552
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0243516 * (1 - exp(-0.387181 * (distance() + 0.324893)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0253552
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal3,metal4,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal3 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.97507 - 0.994142 * (distance())) + 0.0121931 / pow(distance() , 1.25568)) * 1.06387 * pow((width1() + width2()) / 2 , 0.0885597)
]
CAPACITANCE NEARBODY metal3 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-2.97507 - 0.994142 * (distance())) + 0.0121931 / pow(distance() , 1.25568)) * 1.06387 * pow((width1() + width2()) / 2 , 0.0885597)
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal3,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0327669 * (1 - exp(-0.716351 * (distance() + 0.0810567)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0342252
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0127629 * (distance()) + 0.406128) * (0.131703 - exp(-(-0.138215 * (distance()) + 0.269657) * (enclose() + 1.70017 * (distance()) + 4.93099) - (0.0194128 * (distance()) + 0.014292) * (enclose() + 0.52536 * (distance()) + 6.01164) * (enclose() + 0.52536 * (distance()) + 6.01164)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0327669 * (1 - exp(-0.716351 * (distance() + 0.0810567)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.234971 * (0.131703 - exp(0.421418 * (enclose() + 13.4318) - 0.111356 * (enclose() + 8.63844) * (enclose() + 8.63844)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0327669 * (1 - exp(-0.716351 * (distance() + 0.0810567)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0342252
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0127629 * (distance()) + 0.406128) * (0.131703 - exp(-(-0.138215 * (distance()) + 0.269657) * (enclose() + 1.70017 * (distance()) + 4.93099) - (0.0194128 * (distance()) + 0.014292) * (enclose() + 0.52536 * (distance()) + 6.01164) * (enclose() + 0.52536 * (distance()) + 6.01164)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0327669 * (1 - exp(-0.716351 * (distance() + 0.0810567)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.234971 * (0.131703 - exp(0.421418 * (enclose() + 13.4318) - 0.111356 * (enclose() + 8.63844) * (enclose() + 8.63844)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal3,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 tndiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.00994421 * (1 - exp(-0.643399 * (distance() + 0.101378)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0103487
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 tpdiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.00994421 * (1 - exp(-0.643399 * (distance() + 0.101378)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0103487
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal3,metal5,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal3 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.18759 - 0.661268 * (distance())) + 0.0162445 / pow(distance() , 1.12023)) * 1.1137 * pow((width1() + width2()) / 2 , 0.114702)
]
CAPACITANCE NEARBODY metal3 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.18759 - 0.661268 * (distance())) + 0.0162445 / pow(distance() , 1.12023)) * 1.1137 * pow((width1() + width2()) / 2 , 0.114702)
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal3,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0216394 * (1 - exp(-0.389099 * (distance() + 0.300248)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0218102
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00214819 * (distance()) + 0.0432948) * (0.668804 - exp(-(0.121126 * (distance()) + 0.189462) * (enclose() + -0.13314 * (distance()) + 1.65677) - (0.00288376 * (distance()) + -0.00190085) * (enclose() + -0.129593 * (distance()) + 4.85287) * (enclose() + -0.129593 * (distance()) + 4.85287)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0216394 * (1 - exp(-0.389099 * (distance() + 0.300248)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0270179 * (0.668804 - exp(-0.795092 * (enclose() + 0.99107) - 0.0125179 * (enclose() + 4.20491) * (enclose() + 4.20491)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0216394 * (1 - exp(-0.389099 * (distance() + 0.300248)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0218102
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00214819 * (distance()) + 0.0432948) * (0.668804 - exp(-(0.121126 * (distance()) + 0.189462) * (enclose() + -0.13314 * (distance()) + 1.65677) - (0.00288376 * (distance()) + -0.00190085) * (enclose() + -0.129593 * (distance()) + 4.85287) * (enclose() + -0.129593 * (distance()) + 4.85287)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0216394 * (1 - exp(-0.389099 * (distance() + 0.300248)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0270179 * (0.668804 - exp(-0.795092 * (enclose() + 0.99107) - 0.0125179 * (enclose() + 4.20491) * (enclose() + 4.20491)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal3,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0146975 * (1 - exp(-0.381792 * (distance() + 0.305004)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0147727
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0146975 * (1 - exp(-0.381792 * (distance() + 0.305004)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0147727
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal3,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal3 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.56129 - 0.532942 * (distance())) + 0.0167995 / pow(distance() , 1.0288)) * 1.34033 * pow((width1() + width2()) / 2 , 0.126816)
]
CAPACITANCE NEARBODY metal3 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.56129 - 0.532942 * (distance())) + 0.0167995 / pow(distance() , 1.0288)) * 1.34033 * pow((width1() + width2()) / 2 , 0.126816)
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal3,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0159348 * (1 - exp(-0.316584 * (distance() + 0.454336)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0163103
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00082105 * (distance()) + 0.0285083) * (0.886354 - exp(-(0.147811 * (distance()) + 0.167419) * (enclose() + 0.467499 * (distance()) + 0.773993) - (-0.00213503 * (distance()) + -0.015732) * (enclose() + 1.35324 * (distance()) + 2.3765) * (enclose() + 1.35324 * (distance()) + 2.3765)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0159348 * (1 - exp(-0.316584 * (distance() + 0.454336)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0163068 * (0.886354 - exp(-0.906474 * (enclose() + 3.11149) - -0.0264071 * (enclose() + 9.1427) * (enclose() + 9.1427)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0159348 * (1 - exp(-0.316584 * (distance() + 0.454336)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0163103
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00082105 * (distance()) + 0.0285083) * (0.886354 - exp(-(0.147811 * (distance()) + 0.167419) * (enclose() + 0.467499 * (distance()) + 0.773993) - (-0.00213503 * (distance()) + -0.015732) * (enclose() + 1.35324 * (distance()) + 2.3765) * (enclose() + 1.35324 * (distance()) + 2.3765)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0159348 * (1 - exp(-0.316584 * (distance() + 0.454336)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0163068 * (0.886354 - exp(-0.906474 * (enclose() + 3.11149) - -0.0264071 * (enclose() + 9.1427) * (enclose() + 9.1427)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal3,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0173386 * (1 - exp(-0.317926 * (distance() + 0.445108)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0177481
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0173386 * (1 - exp(-0.317926 * (distance() + 0.445108)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0177481
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal4,metal5,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal4 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.29571 - 0.937179 * (distance())) + 0.0123831 / pow(distance() , 1.17036)) * 1.27606 * pow((width1() + width2()) / 2 , 0.0957227)
]
CAPACITANCE NEARBODY metal4 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.29571 - 0.937179 * (distance())) + 0.0123831 / pow(distance() , 1.17036)) * 1.27606 * pow((width1() + width2()) / 2 , 0.0957227)
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal4,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0341924 * (1 - exp(-0.692948 * (distance() + 0.100062)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0363272
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0229897 * (distance()) + 0.549318) * (0.0967393 - exp(-(-0.0674557 * (distance()) + 0.0994161) * (enclose() + 3.73375 * (distance()) + 9.18729) - (0.0104588 * (distance()) + 0.0232735) * (enclose() + 0.791766 * (distance()) + 7.30111) * (enclose() + 0.791766 * (distance()) + 7.30111)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0341924 * (1 - exp(-0.692948 * (distance() + 0.100062)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.332133 * (0.0967393 - exp(0.237862 * (enclose() + 27.856) - 0.0755675 * (enclose() + 11.2599) * (enclose() + 11.2599)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0341924 * (1 - exp(-0.692948 * (distance() + 0.100062)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0363272
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0229897 * (distance()) + 0.549318) * (0.0967393 - exp(-(-0.0674557 * (distance()) + 0.0994161) * (enclose() + 3.73375 * (distance()) + 9.18729) - (0.0104588 * (distance()) + 0.0232735) * (enclose() + 0.791766 * (distance()) + 7.30111) * (enclose() + 0.791766 * (distance()) + 7.30111)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0341924 * (1 - exp(-0.692948 * (distance() + 0.100062)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.332133 * (0.0967393 - exp(0.237862 * (enclose() + 27.856) - 0.0755675 * (enclose() + 11.2599) * (enclose() + 11.2599)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal4,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.00754447 * (1 - exp(-0.619558 * (distance() + 0.125161)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.00799835
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.00754447 * (1 - exp(-0.619558 * (distance() + 0.125161)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.00799835
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal4,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal4 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.70518 - 0.581007 * (distance())) + 0.0137073 / pow(distance() , 1.04109)) * 1.58727 * pow((width1() + width2()) / 2 , 0.122621)
]
CAPACITANCE NEARBODY metal4 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.70518 - 0.581007 * (distance())) + 0.0137073 / pow(distance() , 1.04109)) * 1.58727 * pow((width1() + width2()) / 2 , 0.122621)
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal4,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0225054 * (1 - exp(-0.35645 * (distance() + 0.375848)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.023244
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00311783 * (distance()) + 0.0347471) * (0.781092 - exp(-(0.246453 * (distance()) + 0.227699) * (enclose() + 0.366339 * (distance()) + 1.12973) - (-0.00748718 * (distance()) + -0.00833309) * (enclose() + 0.9399 * (distance()) + 4.00047) * (enclose() + 0.9399 * (distance()) + 4.00047)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0225054 * (1 - exp(-0.35645 * (distance() + 0.375848)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0251681 * (0.781092 - exp(-1.45996 * (enclose() + 2.96143) - -0.045769 * (enclose() + 8.69997) * (enclose() + 8.69997)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0225054 * (1 - exp(-0.35645 * (distance() + 0.375848)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.023244
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00311783 * (distance()) + 0.0347471) * (0.781092 - exp(-(0.246453 * (distance()) + 0.227699) * (enclose() + 0.366339 * (distance()) + 1.12973) - (-0.00748718 * (distance()) + -0.00833309) * (enclose() + 0.9399 * (distance()) + 4.00047) * (enclose() + 0.9399 * (distance()) + 4.00047)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0225054 * (1 - exp(-0.35645 * (distance() + 0.375848)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0251681 * (0.781092 - exp(-1.45996 * (enclose() + 2.96143) - -0.045769 * (enclose() + 8.69997) * (enclose() + 8.69997)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal4,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0117213 * (1 - exp(-0.348062 * (distance() + 0.387134)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0120761
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.0117213 * (1 - exp(-0.348062 * (distance() + 0.387134)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0120761
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal5,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal5 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.38128 - 0.861428 * (distance())) + 0.0146915 / pow(distance() , 1.10181)) * 1.25346 * pow((width1() + width2()) / 2 , 0.101998)
]
CAPACITANCE NEARBODY metal5 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = 3
    max_distance = 3
    C = length() * (exp(-3.38128 - 0.861428 * (distance())) + 0.0146915 / pow(distance() , 1.10181)) * 1.25346 * pow((width1() + width2()) / 2 , 0.101998)
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal5,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0332535 * (1 - exp(-0.622191 * (distance() + 0.134568)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0356674
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00386363 * (distance()) + 0.0643302) * (0.741294 - exp(-(0.0902562 * (distance()) + 0.681135) * (enclose() + -0.131137 * (distance()) + 1.53835) - (0.00721991 * (distance()) + -0.0253688) * (enclose() + -0.651357 * (distance()) + 6.05786) * (enclose() + -0.651357 * (distance()) + 6.05786)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0332535 * (1 - exp(-0.622191 * (distance() + 0.134568)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0418242 * (0.741294 - exp(-1.13242 * (enclose() + 0.882665) - 0.0107308 * (enclose() + 2.80107) * (enclose() + 2.80107)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_distance = 3
    max_enclose = 3
    C = 0.0
    if ((distance() > 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0332535 * (1 - exp(-0.622191 * (distance() + 0.134568)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0356674
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00386363 * (distance()) + 0.0643302) * (0.741294 - exp(-(0.0902562 * (distance()) + 0.681135) * (enclose() + -0.131137 * (distance()) + 1.53835) - (0.00721991 * (distance()) + -0.0253688) * (enclose() + -0.651357 * (distance()) + 6.05786) * (enclose() + -0.651357 * (distance()) + 6.05786)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0332535 * (1 - exp(-0.622191 * (distance() + 0.134568)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0418242 * (0.741294 - exp(-1.13242 * (enclose() + 0.882665) - 0.0107308 * (enclose() + 2.80107) * (enclose() + 2.80107)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal5,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal5 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.00647113 * (1 - exp(-0.563578 * (distance() + 0.164956)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.00692533
    }
]
CAPACITANCE CROSSOVER FRINGE metal5 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_caldistance = 5 // For reference
    max_distance = 3
    C = 0.0
    if (distance() > 0.0) {
        C = length() * 0.00647113 * (1 - exp(-0.563578 * (distance() + 0.164956)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.00692533
    }
]
//attach the following commands to support both xCalibre and XRC tools.
CAPACITANCE CROSSOVER PLATE gate poly
[
    PROPERTY C
      C = 0
]
CAPACITANCE CROSSOVER FRINGE poly tndiff
[
    PROPERTY C
      C = 0
]
CAPACITANCE CROSSOVER FRINGE poly tpdiff
[
    PROPERTY C
      C = 0
]
